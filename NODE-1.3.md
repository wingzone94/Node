# NODE 1.3 — 開発計画書（制作・連携アップデート / Node 1.3 Connect）

> 本ファイルは Node 1.3 の**開発判断の正本**。構想書（2026-07-19 ユーザー提示）を、実際のコードベース構成に接地させた実装計画としてまとめる。
> AGENTS.md / STATUS.md の制約（1タスク1目的・既存記事を壊さない・cybernode.local検証・テスト後ZIP・masterへリリース）を厳守すること。

## 進捗サマリー（2026-08-01時点）

| 段階 | 状態 |
| --- | --- |
| 第1段階: Webhook基盤 + Discord通知 | ✅ **完了**（テーマ1.2.1に同梱してリリース済み。実Discord受信確認済み） |
| 第2段階: X投稿（自動投稿・OAuth連携・タグ付与） | ✅ **実装完了**（plugin 1.3.6。本番でX認証成功確認済み。公開操作を伴う実投稿の初回確認のみ、ユーザーの実行許可待ち） |
| 第3段階: AI共通基盤（node-ai-tools v2.0） | ✅ **完了・コミット済**（`3bcd605`。Core + Provider Adapter 3種 + AI設定画面） |
| 第4段階: 既存AI機能の移行と拡張 | ✅ **完了**（要約・ファクトチェック・自動実行をCore経由へ統一。Qwen/Ollama/`provider=off`対応、生成メタ・Node Connectイベント・再生成警告を実装） |
| 第5段階: 印刷機能（テーマ本体） | ✅ **完了・コミット済**（`44862b3`） |
| 第6段階: 読了バッジを「ブログ平均基準」に統一（テーマ本体） | ✅ **完了**（公開20件以上かつ健全な分布で中央値/p90基準へ移行。母数不足・偏り時は1.2.5絶対閾値へ退避。外れ値回帰テスト済み） |

> 計画外の先行実装: メンテナンスモード（`inc/maintenance.php`、1.2.4でリリース済み。1.3で開始予定時刻、終了時の自動解除・Luminous Coreトップへの遷移、長期カウントダウンの見切れ防止を追加）、記事チェック統合パネル・校正機能・アイキャッチaltの自動付与・記事一覧のFCカム（§14）。§12のテーマ小修正3件も実装済み（未コミット）。
>
> **方針変更**: ファクトチェックは「公開必須」から**推奨**へ（2026-07-26）。検索枠切れや失敗で記事が出せなくなる事故を避けるため、公開はブロックせず、公開直前パネルと記事一覧の ⚠️ / 🚨N で促す。

---

## 0. テーゼ

**「作る、確かめる、届ける、残す。」**

| 概念 | 機能 | 実装先 |
| --- | --- | --- |
| 作る | AIによる記事制作支援（要約・校正・タイトル案・SNS投稿文） | `node-ai-tools` v2.0（AI共通基盤へ改修） |
| 確かめる | ファクトチェック・校正 | `node-ai-tools` v2.0 |
| 届ける | Webhook・Discord通知・X投稿支援 | **新規プラグイン `node-connect`** |
| 残す | 印刷・PDF保存 | テーマ本体（印刷CSS + 印刷ボタン） |

Node 1.2 が読者向け閲覧体験の強化だったのに対し、1.3 は運営者・ライターの「制作 → 確認 → 公開 → 共有 → 保存」フローを支える。Node をブログテーマから「Luminous Core の運営基盤」へ発展させる。

---

## 1. アーキテクチャ方針（テーマ内 / プラグインの役割分担）

### 1.1 配置の決定

| 機能 | 配置 | 理由 |
| --- | --- | --- |
| Webhook基盤・Discord通知・X投稿支援 | 新規 `plugins-embedded/node-connect/` | 投稿イベントと外部送信はテーマ切替後も動くべき機能。既存の embedded プラグイン群（node-series 等）と同じ配布形態（`production_plugins/node-connect.zip`）を踏襲 |
| AI共通基盤（Provider Adapter） | `plugins-embedded/node-ai-tools/` の内部改修 | 既存の `class-gemini-api.php` / meta-box群 / ajax-handlers をそのまま器として使い、内部だけ差し替える。プラグインを増やさない |
| 印刷CSS・印刷ボタン | テーマ本体（`src/styles/_print.css`、`template-parts/`） | 見た目の責務はテーマ。Node 2.0 のレイアウト刷新でもCSSファイル単位で差し替え可能 |

### 1.2 全機能共通の原則（構想書 §2 の実装解釈）

* 設定は既存パターンに従う: サイト全体設定は `get_option`（`node_connect_*` / `node_ai_*` プレフィックス）、ライター個別のAPIキーは既存の user meta 方式（`node_gemini_api_key` の前例）を踏襲
* Webhook URL・APIキーは**フロントHTMLへ一切出力しない**。設定画面は `manage_options` 権限のみ。保存時に `sanitize_url` / trim、表示時は末尾数文字以外をマスク
* 各機能は個別に有効化・無効化できる（機能ごとの enable オプション + 無効時はフック登録自体をスキップ）
* **外部送信の失敗は記事公開を妨げない**: 送信は publish フックから `wp_schedule_single_event()` で非同期化し、本体処理と分離する（既存 `inc/scheduler.php` に cron 運用の前例あり）
* 内部構造は Node 2.0 でそのまま再利用する前提で、表示（テンプレート/CSS）とロジック（イベント・送信・AI呼び出し）を分離する

---

## 2. 第1段階 — Webhook基盤 + Discord通知（`node-connect`）✅ 完了（2026-07-19）

> **実装完了**。テーマ 1.2.1（master `4a2db49`）にベータ版として同梱しリリース済み。§2.5の完了条件はすべて充足（PHPUnit・実Discord受信・build.json再生成）。実運用のWebhook設定（#更新情報宛）もローカル・本番とも投入済み。詳細は STATUS.md の Work Log を参照。

### 2.1 プラグイン構成

```
plugins-embedded/node-connect/
├── node-connect.php              # ヘッダー・定数・ローダー（node-series.php の形式踏襲）
├── includes/
│   ├── class-event-bus.php       # Nodeイベントの定義とdispatch（do_action 'node_connect_event'）
│   ├── class-webhook-sender.php  # 送信キュー・wp_remote_post・タイムアウト・再送・ログ
│   ├── class-discord-formatter.php # イベント → Discord Embed ペイロード変換
│   └── class-delivery-log.php    # 送信履歴（optionに件数上限付きで保存）
└── admin/
    └── settings-page.php         # 外部連携設定画面・接続テスト
```

### 2.2 イベントとWPフックの対応

| Nodeイベント | 発火元WPフック | 備考 |
| --- | --- | --- |
| 記事の新規公開 | `transition_post_status`（→publish、旧≠publish） | 予約公開もここを通る。旧statusが `future` なら「予約記事の公開」として区別 |
| 公開済み記事の更新 | `post_updated`（publish→publish） | リビジョン・自動保存は除外 |
| 記事の非公開化 | `transition_post_status`（publish→draft/private） | |
| 記事の削除 | `wp_trash_post` / `before_delete_post` | 公開済みだったもののみ |
| AI要約の生成完了 / ファクトチェック完了 / AI処理失敗 | `node_ai_tools` 側から `do_action( 'node_connect_event', ... )` | 第4段階でAI側に発火を追加。node-connect側は最初からイベント名を予約 |
| Nodeアップデート通知 | 既存アップデータ（build.json検知）成功後 | |
| メンテナンス開始・終了 | 1.3初期では対象外（イベント名のみ予約） | |

イベントごとに通知の有無を設定できる（イベントID × Webhook URL のマトリクスではなく、**URL登録ごとに「対象イベントのチェックボックス群」**を持たせる単純な形にする）。

### 2.3 Discord通知仕様

* Incoming Webhook + Embed形式。含める情報: 記事タイトル・URL・投稿者名・公開日時・概要（抜粋）・アイキャッチ（`image`）・カテゴリ・シリーズ名（`node_series` term があれば）・更新/新規の区別・AI要約の有無
* Embedカラーはシリーズのプライマリカラー（term meta）→ なければブランドオレンジ `#FF9900`
* **Webhook URL登録は最大3件**（新着/更新/開発通知などの使い分け想定）。設定画面の複雑化を防ぐ
* アイコンに人物・人のシルエットは使わない（プロジェクト方針）

### 2.4 安全設計（必須実装）

* 接続テスト（設定画面からテストEmbed送信）
* `wp_remote_post` タイムアウト 5秒、失敗時は cron で最大2回再送（計3試行）
* 重複送信防止: `post_id + イベント種別 + URL` のハッシュを transient（10分）に記録し同一通知を抑止
* 送信履歴: 直近50件を option に保存（日時・イベント・宛先ラベル・HTTPステータス・成否）。設定画面に一覧表示
* 通知の一時停止トグル（全体 / URL単位）
* 送信失敗は記事公開処理へ波及させない（§1.2）

### 2.5 完了条件

* PHPUnitでイベント判定（新規/予約/更新/非公開/削除の status 遷移分岐）・重複抑止・イベントフィルタをカバー
* cybernode.local で実Discordサーバー（テスト用チャンネル）への通知を確認し、記事URL・編集URLを添えて報告
* `php -l` / `bun x vite build` 通過、ZIP生成時に `build.json` 再生成

---

## 3. 第2段階 — X投稿（`node-connect` 内）✅ 完了（2026-07-19、plugin 1.3.2）

> **改訂（2026-07-19 ユーザー決定）**: 当初は「自動投稿はしない（Web Intent方式まで）」としていたが、**新規公開時の自動投稿を実装する**方針へ変更。テーマに旧実装（`inc/scheduler.php` の `node_post_to_x()`、OAuth 1.0a + API v2）が存在していたため、これを node-connect（`Node_Connect_X_Poster`）へ移管・強化する。
>
> **実装完了**。以下すべて実装・テスト済み（node-connect 46テスト green）。実環境では 3-legged OAuth の「Xと連携」ログインフローで @Luminous_Core_ との連携・認証成功をローカル/本番とも確認済み。残るは次の実記事公開時の初回自動投稿の目視確認のみ。
> 計画からの主な追加実装: ①**「Xと連携」ボタン**（3-legged OAuth 1.0a。Access Token手動発行を不要化、`@アカウント名` 表示・解除ボタン付き。XのCallback URL完全一致要件によりコールバックは固定URL + request_token照合方式）②**固定テンプレ**（ユーザー指定: `ブログ記事を投稿しました\n「{{title}}」\n{{url}}\n{{tags}}`）③**`{{tags}}` プレースホルダ**（記事タグ→ハッシュタグ列）④**X文字数上限への自動適合**（重み280・URL=23換算。要約の自動切り詰め・入り切らないタグの自動脱落）⑤管理画面通知の絵文字撤去（twemojiリンク切れ対策）

* **自動投稿**: `post_published` イベント（予約公開含む）を購読し、テンプレート置換した投稿文を X API v2（`POST /2/tweets`、OAuth 1.0a）へ送信
  * 記事1件につき自動投稿は**生涯1回**（`_node_x_posted` メタで永続抑止。旧実装と互換）
  * 記事ごとの除外チェック（`_node_connect_x_skip`、編集画面のメタボックス）。公開検知から送信まで30秒空け、Gutenbergのメタボックス保存が届いてから最終判定する
  * 記事の**更新では投稿しない**（新規公開のみ）
  * 失敗分類: 通信エラー・5xxのみ最大2回再送（計3試行）。4xx（認証・重複・権限・レート）は即打ち切り。結果は送信履歴に記録
* 認証情報は旧オプション名（`node_x_api_key` 等）を引き継ぎ、設定UIは外部連携設定へ移設（Luminous Settings の旧セクションは撤去）。設定画面に認証テスト（`GET /2/users/me`、投稿なし）
* 投稿テンプレート設定（`{{title}}` `{{url}}` `{{summary}}` `{{category}}` プレースホルダ置換。旧実装と同形式・`{{summary}}` はAI要約優先）
* メタボックスに生成文プレビュー + Web Intent（`https://twitter.com/intent/tweet?text=...`）の**手動投稿も併設**
* ハッシュタグ候補・投稿文のAI生成は第4段階のAI基盤連携で追加（それまではテンプレート置換のみ）

---

## 4. 第3段階 — AI共通基盤（`node-ai-tools` v2.0）✅ 完了（2026-07-19）

> **実装完了**。§4.1の3層分離をそのまま実装（`includes/class-ai-core.php` / `includes/providers/` の interface + Gemini / Qwen / Ollama、`admin/settings-page-ai.php`）。`composer test` 全225テストgreen、cybernode.local で Gemini 実キー接続・Core経由の実ファクトチェック・利用履歴記録・実Ollama生成を確認済み（Qwenは実キー未入手のためモック単体テストまで＝§4.5の規定に準拠）。詳細は STATUS.md の Work Log を参照。
>
> 計画からの差分: ①`Node_AI_Provider` に `get_model()` を追加（計画の3メソッド → 4メソッド。利用履歴にモデル名を記録するため）②Gemini にサイト共通キー option `node_ai_gemini_api_key` を追加（user meta と定数の間の優先度）③`fact_check()` は Gemini 時のみ Google Search グラウンディングを使い、他プロバイダーでは知識範囲での抽出支援プロンプトへ自動切替
>
> **リリース反映（2026-08-01）**: プラグインヘッダーを Version 2.0.0 へ更新済み。既存メタボックス・AJAX・cronのCore移行は第4段階で完了した。

### 4.1 現状と改修方針

現在の `includes/class-gemini-api.php` は Gemini 専用クラスで、`generate_content` / `generate_summary` / `fact_check` / `generate_reading_time_estimate` を持ち、APIキーは user meta `node_gemini_api_key` から取得している。これを次の3層に分離する。

```
Node AI Core（class-ai-core.php）
  ├─ 機能API: summarize() / fact_check() / proofread() / suggest_titles() / social_post()
  ├─ プロバイダー解決・利用履歴記録・エラー正規化
  ↓
Provider Adapter（interface Node_AI_Provider）
  ├─ class-provider-gemini.php   ← 既存 class-gemini-api.php を薄く包む（ロジック移植）
  ├─ class-provider-qwen.php     ← OpenAI Compatible API（chat/completions）
  └─ class-provider-ollama.php   ← ローカル /api/chat（キー不要・URL可変）
```

* `Node_AI_Provider` インターフェース: `generate( string $prompt, array $options ): string|WP_Error` + `test_connection(): true|WP_Error` + `get_label()` の最小3メソッド
* 既存の meta-box（ai-summary / fact-check / featured-image-ai）と ajax-handlers は Core の機能APIを呼ぶ形へ書き換え、**表示・保存仕様は変えない**（回帰ゼロが第4段階のゲート）

### 4.2 プロバイダー仕様

| | Gemini（標準・推奨） | Qwen | Ollama |
| --- | --- | --- | --- |
| 認証 | APIキー（既存user meta継続 + サイト共通キーのoption追加） | APIキー + エンドポイントURL | 不要 |
| モデル | 既定 Gemini 3.1 Pro Preview 相当。**コードに固定せず option で差し替え可能**（既存 `inc/gemini-models.php` の一覧管理を流用） | 推奨1種のみ表示（内部IDはoption） | Gemma / Qwen の2択。任意モデルIDは「詳細設定」に分離 |
| 接続先 | Google API固定 | OpenAI互換エンドポイント（可変） | 既定 `http://localhost:11434`（可変） |

### 4.3 AI設定画面

プロバイダー選択（Gemini推奨 / Qwen / Ollama / 使用しない）→ 選択したプロバイダーの項目だけ表示。各プロバイダーに接続テストボタン。詳細なモデルIDは常時表示しない。

### 4.4 利用履歴

実行のたびに記録: 機能・プロバイダー・モデル・日時・成否・対象記事ID・（取得できれば）トークン数。optionまたはカスタムテーブルに直近200件上限で保存し、設定画面に月間実行回数と一覧を表示。**料金の正確な計算はしない**（構想書 §4.9）。

### 4.5 完了条件

* 3アダプターの接続テストが実環境で通る（Gemini=実キー、Qwen=互換API、Ollama=ローカル起動して確認。Ollama未導入環境ではモック単体テストまで）
* 既存のAI要約・ファクトチェックUIに回帰がない（PHPUnit + cybernode.local実保存確認）

---

## 5. 第4段階 — 既存AI機能の移行と拡張 ✅ 完了（2026-08-01）

> **実装完了（2026-08-01）**。手動要約・手動ファクトチェック・cronファクトチェック・公開後の自動要約を `Node_AI_Core` 経由へ統一。`provider=off` を全経路で尊重し、Qwen/Ollamaの実行可否判定、生成日時・プロバイダー・モデルの内部記録、古い要約警告、再生成前確認、Node Connectの完了/失敗イベントを追加した。公開時の同期AI呼び出しは30秒後のcronへ移し、記事公開処理と外部APIを分離した。

1. Intelligence Summary（既存AI要約）を Core 経由に移行。生成結果は自動公開しない・記事更新時も自動上書きしない現行仕様を維持し、「再生成確認」「古い要約の警告」（生成日時 vs 記事更新日時の比較表示）「使用モデル・生成日時の内部記録」を追加
2. ファクトチェックを Core 経由に移行。**確認箇所の抽出支援**という位置づけを出力プロンプトに明文化（真偽の最終判定はしない）
3. 校正を既存の記事チェックUIから利用可能にし、タイトル案・SNS投稿文はCore機能APIとして追加。専用UIは§14.6の決定どおりNode 1.3対象外とする
4. AI完了/失敗イベントを `node_connect_event` へ発火（第1段階で予約済みのイベントを接続）
5. `node_ai_author_has_api_key()` を「選択中プロバイダーが実行可能か」の判定へ拡張する（Ollama選択時はキー不要）。`provider=off` は手動・自動の全経路で無効化する

---

## 6. 第5段階 — 印刷機能（テーマ本体）✅ 完了（2026-07-20）

> **実装完了**。§6.1のとおり `src/styles/_print.css`（style.cssエントリの最後に@import）+ 印刷ボタン（シェアボタン群、`window.print()` のみ）+ 印刷専用要素（ブログ名ヘッダー / 記事URL・著作権フッター、`inc/print.php` の `node_print_the_header/footer()`）。設定は Luminous Settings に「印刷」セクション（有効化・ボタン位置 先頭/末尾・URL/著作権表示）を追加。§6.2の3パターン（長文477・表482+コード/表テスト記事・脚注1169）× ライト/ダーク × PC/モバイル/タブレット幅で印刷PDFを確認済み。
>
> 計画からの差分・発見: ①**FOUC対策のインラインstyle（functions.php `node_critical_inline_styles`）の `html[data-theme="dark"]` 背景指定が印刷の白背景に勝ってしまうバグを発見**し、背景指定を `@media screen` 限定に修正（動的カラーのインラインstyleも同様に勝ち、さらに body にも data-theme が付与され body 上で変数が再定義されるため、`_print.css` の変数上書きは `html:root[data-theme]` + `html body[data-theme]` まで対象を広げた）②AI要約（Intelligence Summary）カードは**印刷に含めない**（ユーザー指示。当初の強制展開から変更）③読了時間・文字数バッジは印刷では**常に展開済み**で出す（A4紙面幅≈794pxが1000px以下のアイコンのみ化メディアクエリに入るため明示上書き。ユーザー指示）④本文中のテキストリンクは印刷時にURLを併記
>
> **残作業**: 未コミット。印刷ボタンは既定で有効（オプション `node_print_enabled` 既定 '1'）。

### 6.1 実装

* `src/styles/_print.css` を新設し `@media print` でスタイル定義（vite ビルドに載せる）。既存の追従UI（`_fab.css`）・ナビ・シェア・コメント・広告・アニメーション・装飾背景を `display: none`
* 表示する: ブログ名・タイトル・概要・公開/更新日・投稿者・本文・見出し・画像・キャプション・表・引用・脚注・コード・参考リンク・記事URL・著作権表記。記事URLと著作権は印刷時のみ表示する要素として `single.php` 側に追加
* 1カラム固定・白背景黒文字・ダークモード変数を強制ライト値へ上書き・`break-inside: avoid`（見出し直後・表・コード・図版）・画像 `max-width: 100%`
* 印刷ボタンは記事下部（シェアボタン群の並び）に配置し `window.print()` を呼ぶだけ。追従ボタンは増やさない
* PDFはブラウザの「PDFとして保存」に委ねる。サーバー側PDF生成はしない

### 6.2 完了条件

* Chrome/Safari の印刷プレビューで、長文記事・コード記事・表/脚注入り記事の3パターンを確認（ライト/ダーク両モードから印刷してダーク色が漏れないこと）
* PC・スマートフォン・タブレット幅で印刷プレビュー確認、cybernode.local の実記事URLを添えて報告

---

## 7. 管理画面の構成

設定は3領域に分ける（構想書 §6）。

* **外部連携**（node-connect 設定画面）: Webhook有効化・Discord URL（最大3件）・イベント選択・テスト通知・X投稿テンプレート・送信履歴・一時停止
* **AI**（node-ai-tools 設定画面）: AI有効化・プロバイダー選択・キー/接続先・接続テスト・要約/ファクトチェック設定・利用履歴
* **印刷**（既存の Luminous Settings ページ = `inc/admin-settings.php` に1セクション追加）: 印刷機能の有効化・ボタン表示位置・記事URL/著作権表記の表示

各設定に1行説明を付け、専門用語を避ける。

---

## 8. Node 1.3 で実装しないもの

構想書 §8 のとおり。特に以下は**実装禁止**として明記する。

* ~~Xへの自動投稿~~（2026-07-19 ユーザー決定で解禁 → §3。ただし**記事更新時の再投稿・スレッド投稿・画像添付投稿は対象外のまま**）
* Gemini / Qwen / Ollama 以外のプロバイダー、多数モデルの並列サポート
* AIによる事実の最終判定・記事の完全自動投稿
* 独自PDF生成エンジン、サーバー側での大規模ローカルAI実行
* Slack / Teams など Discord 以外の通知先（イベント基盤は汎用に作るが、フォーマッタは Discord のみ）
* 複雑なワークフロー自動化、AI画像生成、学習用データセット管理

---

## 9. リリース・検証規約（プロジェクト既定ルールの再掲）

* バージョンは **1.3.0**。`style.css` を正としてREADMEバッジ含む4箇所を同期。プラグイン側（node-connect / node-ai-tools）は各自のバージョンを更新
* リリースは必ず **master** に載せる（自己更新が master 参照）。ZIP生成のたびに `build.json` を再生成する
* ビルドフロー: `bun x vite build` → cybernode.local で検証 → ZIP。検証前に該当URLの Super Cache をパージ
* 検証報告には記事URL・編集URLを必ず添える
* 各段階の完了ごとに STATUS.md の Work Log へ記録する
* 段階間の依存: 第1段階（イベント基盤）→ 第2段階と第3段階は並行可 → 第4段階は第1・3段階に依存 → 第5段階は独立（いつでも並行可）

---

## 10. Node 2.0 との役割分担

1.3 = 機能と運営基盤の強化、2.0 = UI/レイアウト/閲覧体験の刷新（正本は NODE-2.0.md）。1.3 で作る Webhook・AI・印刷の**内部処理（イベントバス・Provider Adapter・印刷用マークアップ）は 2.0 でそのまま再利用**できるよう、表示層と分離して実装する。2.0 の2カラム化後も印刷は1カラムへ変換する前提で `_print.css` を本文中心の独立レイアウトとして書く。

---

## 11. 名称

正式名称は **「Node 1.3 Connect（制作・連携アップデート）」** とする。告知の3本柱は「外部サービス連携」「選べるAI連携」「印刷対応」。

---

## 12. テーマ小修正（1.3同梱）

主要5段階とは別に、1.3リリースへ同梱するテーマ本体の軽微修正。

| # | 修正 | 状態 |
| --- | --- | --- |
| 12-1 | **検索クリアのアニメーション改善**: 読了ゲージ100%到達時の破壊演出（`playBarShatterAnimation`）と同系の**シャード（破片）バースト**を採用（最終決定）。クリック時に文字は0.2秒の高速フェード（blur 2px、expo系イージング）で消え、同時にキーワードの文字幅を実測した範囲からダークモードは純白・ライトモードは入力文字と同色の破片（白は明るい背景に溶けるためモード別に切替）（文字幅に応じ8〜20個、5px角）が上半円方向へ弾け飛ぶ（30〜90px・回転±360°・scale 0へ、0.8〜1.2秒、Web Animations API・終了後自動削除）。破片はoverflowクリップ回避のため body 直下に fixed 配置。連打ガード付き。対象: `src/styles/_header.css`（`.is-dissipating` / `.m3-search-shard`）、`header.php`（`spawnShards()` / `animateClear()`）。※検討経緯: mask-imageワイプ→シャード→スライド退場→吸い込み→一文字バックスペース→ノイズ溶解を試作し、シャードへ回帰して確定 | ✅ 実装済み（2026-07-19、cybernode.local動作確認済み） |
| 12-2 | **検索クリアボタンのデザイン変更**: 回転ポップアニメーション（`is-popping` / `m3-search-clear-pop`）を削除し、**薄いオレンジのティントサークル × オレンジアイコン**のトーナル配色（`color-mix` で primary 14%、hover 26%。28pxサークル・16pxグリフ。テーマの `--md-sys-color-primary` 参照でダークモードにも自動追従）へ変更。`.m3-header .m3-icon-button` の `background: transparent !important` に勝つためセレクタを `#m3-search-clear` に変更し、モバイル幅の flex-basis 上書きも `flex: 0 0 28px` で打ち消して正円を維持。対象: `src/styles/_header.css`、`header.php`（is-popping付与JS削除） | ✅ 実装済み（2026-07-19、cybernode.local動作確認済み） |
| 12-3 | **無限スクロールのローディング表示変更**（node-flowプラグイン）: 回転する同期アイコン（Material Symbols `autorenew`）を廃止し、テーマの `m3-spinner` と同系の**円形プログレスインジケーター**（SVGサークル、ブランドオレンジ、回転1.4s + dashアニメーション）へ変更。「読み込み中...」テキストは縦積み配置で維持。対象: `plugins-embedded/node-flow/assets/js/scroller.js` | ✅ 完了（2026-08-01、cybernode.localで実トリガー発火・REST 200・記事16→26件への追加・回転/dashアニメーション・コンソールエラーなしを確認） |

---

## 13. 読了バッジを「ブログ平均基準」に統一（第6段階・テーマ本体） ✅ 完了（2026-08-01）

> **実装完了（2026-08-01）**。公開記事20件以上・中央値500字以上・p90が中央値の10倍以内を相対基準の採用条件とし、中央値=60%、p90=100%の同一進捗から色・ラベル・ゲージを決定する。条件を満たさない場合は1.2.5の絶対閾値へ完全フォールバックする。分布は24時間キャッシュし、投稿保存・削除・ステータス変更時に無効化する。

**方針決定: 2026-07-24（ユーザー指示・議論を経て確定）。1.3 で実装する。**

### 13.1 思想 — なぜ平均基準か
現行（1.2.5）の判定は**絶対文字数の固定閾値**（短い<1,500 / やや短い<3,000 / 標準<6,000 / やや長い<10,000 / 長い≥10,000）に基づく。ゲージもこのランクに同期した固定段階（20/40/60/80/100%）で描く。

問題は、この閾値の数字に**根拠がない（恣意的）**こと。「10,000字で長い」という線は誰かが引いただけで、そのブログの現実を反映していない。つまり固定閾値は「何をもって長いとするか」を**でっち上げている**——小さな嘘を含む。

対して**ブログ全記事の文字数分布から基準を導けば、その恣意性がなくなる**（「満タン＝どこか」を実データが決める）。基準の出どころが現実にあるという意味で、平均基準の方が**誠実**。これが採用理由。

### 13.2 決定事項 — バッジ全体を平均基準に統一する
ゲージだけを相対に、色・ラベルを絶対のまま残すと、**1つのバッジが2つの基準を語り矛盾する**（例: 絶対では「長い＝赤」だが、長文だらけのブログでは相対ゲージは控えめ→色と針がズレる）。中途半端な混在は不可。

したがって、**判定カラー・ランクラベル・円ゲージのすべてを同一の「ブログ平均基準」から導出し、一貫させる**。

- ブログ全体（公開記事）の文字数分布を集計し、**内部基準**（代表値＝後述の安定化を施した中心傾向、および分布の広がり）を求める。
- **基準値そのものはUIに表示しない**（ゲージ位置・色・ラベルの算出に使う内部データ）。
- 各記事は、この分布上のどこに位置するか（平均比・パーセンタイル等）で **ゲージ充填量・判定カラー・ランクラベルが同時に決まる**。色と針とラベルは常に同じ物語を語る。

### 13.3 安定化 — 必須ゲート（ここが実装の生命線）
平均基準は「基準の出どころ」は誠実だが、**その平均自身が母数不足・偏りで嘘をつくと本末転倒**。1.2.0で相対方式を捨てたのは、まさに「テスト環境や記事構成の偏りで平均・最大が少数記事で乱高下し、基準が振れた」ため（絶対文字数へ切替。[[node-v1-1-1-dev]] / CHANGELOG 参照）。**同じ失敗を繰り返さないため、以下を実装の合否ゲートとする**:

- **母数不足時のフォールバック**: 公開記事が一定数（例: 20〜30本）に満たない間は、現行の絶対閾値へ退避する（相対基準が「本当に正直な平均」になるまで使わない）。
- **外れ値に強い代表値**: 素朴な平均（mean）は極端な長文1本で歪む。**中央値やパーセンタイル**を基準に、上限クリップ（分布の上側は p90〜p95 で頭打ち）等を併用する。ゲージ100%＝分布上側の実在ライン。
- **キャッシュと再計算**: 集計は transient でキャッシュし、記事の公開・更新・削除で無効化。全記事走査の負荷を避ける。
- ゲート: 少数記事・極端記事を混ぜたケースで基準が乱高下しないこと（1.2.0の乱高下の再現テストを回帰として持つ）。

### 13.4 実装メモ
- **既存の未使用関数を再活用**: `node_get_global_average_chars()` / `node_get_global_max_chars()`（[inc/utilities.php](inc/utilities.php)、transientキャッシュ付き。1.2.0のゲージ固定閾値化で呼出しが外れ、現在は定義のみ残る）。ただし素朴な mean / max のままでは 13.3 を満たさないため、中央値・パーセンタイル版へ拡張する。
- 判定ロジックの正本 `node_get_article_ranking_info()`（[inc/utilities.php](inc/utilities.php)）を、絶対閾値ベースから平均基準ベースへ改修（色・ラベル・progress を同一基準から算出）。1.2.5時点の gauge は `rank_progress_map` の固定段階なので、これも相対算出へ置換。
- **「ブログ全体データの平均化」は分析用途（1.3の"作る/確かめる"側）でも活きる**が、読者向けバッジとは別物として整理してよい。本節はあくまで読者向けバッジを平均基準へ統一する話。

---

## 14. 記事チェック（2026-07-26 実装）

第4段階の一部として、エディタ右ペインに**ファクトチェックと校正を統合した「記事チェック」パネル**を新設した。

### 14.1 なぜクラシックメタボックスではないのか

`add_meta_box( ..., 'side', ... )` はブロックエディタでは効かない。context に関わらず**下部の「メタボックス」ドロワーへ落ちる**（実機で確認）。右ペインに出すには `PluginDocumentSettingPanel` が必要。

### 14.2 機能

* **統合パネル** — ファクトチェックと校正の指摘を1本のリストへまとめ、重要度順に並べる。観点は Dashicons のアイコンと左ボーダー色で区別
* **公開フロー表示** — 下書き保存 ▶ FC（任意）▶ 公開／予約投稿。状態を色で示す
* **校正（Core 経由）** — 誤字脱字 / 文法 / **言葉遣い（慣用句の誤用・重言・ら抜き・二重敬語）** / 表記ゆれ / 読みやすさ / 解釈違いの6種別
* **判定5段階** — 正確 / おそらく正確 / 要確認 / おそらく不正確 / 検証困難
* **環境連動ラベル** — 検証環境では公開リスクの言い回しへ（例「本番ではアウト」）。`node_ai_is_production` フィルタで上書き可
* **Markdown 持ち出し** — 統合結果をコピー / `.md` 書き出し。http 環境向けに `execCommand` フォールバックあり
* **記事一覧カラム** — ⚠️（未実施）/ 🚨N（未対応 N 件）/ ✅
* **アイキャッチ alt の自動付与** — 保存30秒後に cron。**alt が空のときだけ**動作し、既存 alt は上書きしない

### 14.3 撤去した機能と理由

* **画像の出典特定** — Gemini のグラウンディングは逆画像検索ではなく「画像内の文字で通常検索」する。自作アイキャッチに他社の記事URLを出典として提示する誤答を実測。誤帰属は出典なしより有害なため不採用
* **画像の内容チェック** — 精度が実用に届かず撤去（2026-07-26）
* **ChatGPT 連携（プロンプト生成）** — 手動運用へ方針変更。Pro / ChatGPT を使いたい場合はブラウザで行う

### 14.4 Gemini API の堅牢化（実運用で判明した問題）

| 問題 | 対処 |
| --- | --- |
| `parts[0]` 決め打ちで思考パートや分割応答を取りこぼし JSON 破損 | thought 以外の text パートを全連結 |
| MAX_TOKENS の部分応答を成功として返し、壊れた JSON を解析していた | 途中切断は本文があってもエラーにする |
| 429 の復帰時刻を retryDelay だけで判断し、日次上限でも「59秒後に解除」と虚偽表示 | `quotaId` の PerMinute / PerDay で分類 |
| 分類不能な 429 で丸一日モデルを締め出していた | 停止は最長10分に制限し自動回復 |
| 検索グラウンディングの枠を通常生成と混同 | `モデル名#grounding` で別管理 |
| 検索枠切れ時に検索なしで再実行し、裏取りなしの結果を成功として返していた | フォールバックを廃止し中止する |

### 14.5 モデル一覧の整理

30件 → **6件**（Flash 系のみ）。除外: 2.5 未満 / 枝番スナップショット / `-latest` 浮動エイリアス / 特殊用途 / 安定版がある preview / 実行時 404 の提供終了モデル / **Pro 系**。

Pro 系は 2026-04-01 以降に無料枠から外れており、選んでも必ず 429 になるため除外した（`node_gemini_allow_pro_models` フィルタで解禁可）。思考量（High / Low）はモデル一覧から分離し、**指定が効く Gemini 3 系選択時のみ**表示する（保存形式 `<モデルID>@<思考量>` は 1.2 系と互換）。

### 14.6 残課題

* ✅ 要約・ファクトチェックの `Node_Gemini_API` 直呼びは解消し、Core経由へ移行済み。
* `suggest_titles` / `social_post` の専用UIはNode 1.3の実装対象外。Core APIは将来連携用として維持する。

---

## 15. HEADLINE をカルーセル化（トップページ・テーマ本体）✅ 完了（2026-08-01）

**課題**: トップページの HEADLINE（速報）が2列リスト×4件で縦に伸び、ライターが書いた LATEST（新着記事）がファーストビューから押し出されていた。

**方針**: HEADLINE を**横スクロール1段（カルーセル）**へ変更し、縦幅をカード1枚分に固定する。件数はむしろ 4 → 8 件へ増やし、縦幅ではなく横方向に情報量を持たせる。

### 15.1 実装

| ファイル | 役割 |
| --- | --- |
| `template-parts/headline-carousel.php`（新規） | `WP_Query` で速報5件（最大5件）。`role="region"` + `aria-roledescription="カルーセル"`。先頭のアイキャッチのみ `fetchpriority="high"` / `loading="eager"` |
| `src/styles/_headline-carousel.css`（新規） | `@layer components` / BEM（`.c-headline-carousel` `.c-headline-card`）。CSS Scroll Snap（`x mandatory`）。カード幅は `clamp()` で**コンテナ幅を割り切らない値**にして右端に次カードを見切れさせる（Peek） |
| `src/scripts/headline-carousel.js`（新規） | Vanilla JS / ES Module。`scrollBy({behavior:'smooth'})`、端点で `disabled` + `aria-disabled`、`ArrowLeft/Right`、`ResizeObserver` で再判定。外部ライブラリ不使用 |
| `index.php` | HEADLINE のインライン実装を `get_template_part()` に置換 |

### 15.2 設計判断

* **JS は `main.js` の静的 initializer**（遅延 import ではない）。ナビボタンを後出しで表示するとヘッダー行がずれて CLS になるため、初期HTMLでナビを描画し `prev` を `disabled` 済みで出す。
* **HEADLINE だけ角丸を全廃**（セクション外枠・カード・アイキャッチ枠・ナビボタン・アバター・「すべて見る」）。**カテゴリピルは例外で角丸（ピル）のまま**残す — カテゴリバッジは記事一覧・アーカイブ・関連記事にも同じ形で出るため、HEADLINE だけ四角にすると統一性が崩れる（2026-08-08 ユーザー指示で差し戻し）。他セクションが角丸のままなので、直線基調のブロックとして対比させ全体のバランスを取る（2026-08-01 ユーザー指示）。スコープは `.c-headline-carousel` 配下のみで、`/category/ニュース/` の HEADLINE アーカイブには波及させない。
* **ナビボタンはカード列の上に重ねて左右中央配置**（`.c-headline-carousel__stage` を `position: relative`、nav は `inset: 0` + `pointer-events: none`、ボタンのみ `pointer-events: auto`）。ヘッダー行に置くとカード列と無関係な位置になり見栄えが悪いため（2026-08-01 ユーザー指示）。
* **スマホはナビボタン非表示**（≤600px）。指スワイプで足りるうえ、既存の `_layout-fixes.css` のモバイル見出し行と競合させない。
* **アイキャッチはトリミングせず全体表示**。速報のアイキャッチは文字入りのバナー画像が多く、`cover` だと見出し文字が切れるため。当初は 16:9 枠 + `object-fit: contain` にしたが、縦横比の違う画像で帯（余白）が出たので、**枠の比率固定をやめて画像の縦横比に追従**させた（`height: auto`、背景は透明）。リスト表示のサムネは日付列を揃えるため幅96pxの枠だけ確保し、高さは画像なり。
* **「すべて見る」はヘッダーから削除**（2026-08-02 ユーザー指示）。速報一覧への導線はカテゴリアーカイブ側に任せる。`node_get_headlines_url()` は `/headlines/` の301で使うため残す。
* **長いカテゴリ名は長体（scaleX）で詰める**。和文フォントは `font-stretch` が効かないため、`headline-carousel.js` がバッジ幅と自然幅を実測し `--category-squeeze` を算出、内側 `<span>` に `transform: scaleX()` を掛ける。下限 0.72 でそれ以上は従来どおり ellipsis。Webフォント適用後（`document.fonts.ready`）と `ResizeObserver` で再計測する。
* **アイキャッチ未設定のカードは自動縮小**。空の画像枠を出さず、`--no-image` 修飾子で `flex-basis` を約 0.7 倍に縮め、タイトルのクランプを6行へ広げる。カード高さは flex の stretch で揃うため、列の縦幅は変わらない。
* **タイトルは3行クランプ**。`verify:visual` は line-clamp を `vertical-text-clipping` として検出するが、カード高さを揃えて縦幅を固定するための意図的なトリミング。
* カードの `min-width: 0` は必須。長いカテゴリ名（`white-space: nowrap`）が min-content 幅を押し上げ、カード1枚だけ横に伸びる現象を実測したため。

### 15.3 カード表示の整列（2026-08-02）

画像の縦横比に枠を合わせた結果、カードごとに写真の高さが変わり、その下のカテゴリ・タイトル・著者の行がバラバラになって全体が散らかって見えていた。**写真は自由・文字は一直線**という原則で整理した。

* 画像枠に `flex: 1 1 auto` を与えて残り高さを取り、写真はその中で**天地中央**（`max-height: 100%` + `contain`）。周囲に出るのはカード面と同色の余白だけで、別色の帯にはならない
* 文章ブロックは `justify-content: flex-end` で**下寄せ**。あわせてタイトルに `min-height: 3行` を与えたので、**カテゴリ行・タイトル行・著者行が全カードで同じY座標に並ぶ**（実測: バッジ302 / タイトル328 / 著者398 で5枚とも一致）
* 日付は `margin-inline-start: auto` で右端そろえ。バッジの文字数で位置が動かない
* アイキャッチなしのカードはタイトルを6行 → **3行**に統一（行位置を合わせるため）。上半分が空くので、面にごく浅いグラデーションをかけて「空っぽ」ではなく意図した余白に見せる

### 15.3.1 カード表示のカテゴリはタイトル下・複数表示（2026-08-08）

カテゴリはメタ行（日付の左）に**1件だけ**出しており、長い名前は長体（scaleX）で潰して1行に収めていた。潰した文字は読みにくく、記事が持つ他のカテゴリも見えない。**タイトルの下へ移し、複数表示 + 2段折り返し**に変更した（ユーザー指示）。

* **DOMは2箇所に置く**。リスト表示は `[バッジ][タイトル][日付][サムネ]` の並びを `display: contents` + `order` で作っており、バッジがメタ行にある前提で組んである。カード表示用のバッジ（`--tag`）をタイトル下に別途出し、**モードごとに片方を `display: none`** にする。リスト表示のレイアウトには一切触らない
* **長い名前は長体で潰さず2段で折り返す**（`-webkit-line-clamp: 2` + `overflow-wrap: anywhere`）。カード表示では `--category-squeeze` を使わない
* **代わりにバッジの件数を自動制限する**。折り返しでバッジ1個が縦に伸びるため、件数固定だとカードごとに高さが変わる。`limitCategoryBadges()` が**2段を超えたら後ろから1つずつ `hidden`** にして段数を2段以内に収める（PHP側の上限は4件）。実測: 実データは3〜4件が全件2段以内に収まり、7件まで注入したストレス時は 3〜5件へ自動で減った
* **カテゴリ枠の高さは常に2段ぶんで固定**する（`height: calc(18.5px × 2 + 4px)` = 41px、`overflow: hidden`）。段数がカードによって変わるとタイトル・日付のY座標がずれ、§15.3 で揃えた「文字は一直線」が崩れるため。1段で足りるカードには空きができるが、**行位置の一致を優先する**（2026-08-08 ユーザー決定）
* 枠が固定高になったので、`limitCategoryBadges()` の判定は**段数ではなく実高さ**（`scrollHeight > clientHeight`）で行う。名前が2行に折り返したバッジは `offsetTop` が1段ぶんしかないため、段数で数えると枠から溢れても気付けない
* 実測（cybernode.local・PC 1440px）: 全5カードで枠 41px・はみ出し 0・タイトルY 686 / 日付Y 664 / 著者Y 811 が一致。カテゴリを最大7件まで注入したストレステストでも全カードはみ出し 0（表示は1〜6件へ自動調整）
* **入り切らなかった件数は `+N` チップで示す**（2026-08-08 追加。本番 1.2.5 のカードにあった `.m3-label--category-more` と同じ役割）。黙って隠すと「まだカテゴリがある」ことが読者に伝わらないため復活させた。数は **PHP 側で切った分（4件超過）＋ JS が枠に合わせて隠した分**の合計。色はカテゴリ色ではなく面の色にして、本物のカテゴリと取り違えないようにする。リスト表示では出さない（メタ行のバッジ1件だけを使うため）
* **枠の高さは 42px**（1段 = 実測19px × 2 + gap 4px）。18.5px で計算していたときは丸め差で1pxだけ切れていた
* **最後の1件でも収まらない場合だけ折り返しをやめる**（`is-tight`）。名前が2行に折り返す長いカテゴリと `+N` チップは同じ枠に同居できないため、**「長い名前を全部見せる」より「他にもあると伝える」を優先**して1行 + ellipsis に落とす。実測: 「JR East Train Simulator（JR東日本トレインシミュレーター）」のカードは1行 + `+3` になり、はみ出し0
* 実測（カテゴリ最大7件のストレス込み）: 全カードで枠42px・はみ出し**0**、`+2` / `+3` / `+5` / `+6` が正しく出る

### 15.4 表示切替（カード ⇄ リスト）

トップの HEADLINE に表示切替を追加（2026-08-01 ユーザー指示）。**同じDOMのまま CSS だけで組み替える**（`data-view="carousel|list"`）ので、再クエリも重複出力もない。

* 状態は `localStorage`（`src/storage.js` 経由 = `m3_store_headline-view`）で永続。**描画前に反映する数行のインラインスクリプト**をセクション先頭に置き、DOMContentLoaded 後に組み替わることによるレイアウトシフトを防ぐ
* リスト表示ではナビボタンを隠し、スクロールスナップを解除
* **リスト行の構成**: `[バッジ][タイトル][日付][サムネ]`。`body` / `meta` を `display: contents` にして、バッジ・タイトル・日付を行の直接の子として並べ替えている（PC幅のみ）。整列のため ①サムネは**右端**（左に置くと画像なしの行だけ字下げの穴が空く）②バッジ列を128px固定（タイトルの開始位置が一直線になる。長い名前は既存の長体処理が詰める）③画像なしの行には `::after` でサムネ幅の空き枠を作り日付の右端を揃える ④全行 `min-height: 72px` で縦のリズムを固定
* ホバーは行の左に3pxの色帯（`::before`）。字下げや浮き上がりのような**再レイアウトを起こす演出は使わない**
* 著者はカード表示のみ。行では情報密度を優先して省く
* **リスト表示だけの仕様**（2026-08-02 ユーザー指示）: 表示を**4件**に減らす代わりに、長いカテゴリ名を**最大3行**まで折り返して**省略を極力出さない**。件数は CSS（`:nth-child(n+5)` を `display: none`）で絞るのでクエリは共通のまま5件
* 折り返しだけで3行に収まらない場合は `fitListCategoryLabel()` が長体率を 1.0 → 0.6 まで 0.04 刻みで下げる。**内側スパンの論理幅を `幅 ÷ 長体率` に広げてから `scaleX(長体率)` で戻す**ので、見た目の幅は変えずに1行へ入る文字数だけが増える（カード表示の1行用ロジックとは別関数）
* 実装でハマった点2つ: ①`calc(100% / var(--category-squeeze))` は効かない（var での除算が無効）ので幅は JS が px で指定する ②バッジが `inline-flex` なので、広げた幅が `flex-shrink` に押し戻される。内側スパンに `flex: 0 0 auto` と `max-width: none` が必須
* スマホのリスト表示は行内を「バッジ+日付 / タイトル」の2段にする（1行だとタイトルが潰れる）
* **モバイルで実際に踏んだ不具合**: `_layout-fixes.css`（utilities レイヤー）の見出し `white-space: nowrap` により「速報」が箱の外へはみ出し、切替ボタンに重なって**タップできなくなっていた**（`elementFromPoint` で検出）。components 側の font-size 指定はレイヤー順で負けるため、修正は `_layout-fixes.css` 側に置いた
* アイコンは `grid_view` / `reorder`（`view_carousel` / `view_list` から変更）
* **形が変わるのは中身だけ**。セクション枠・切替ボタン・「すべて見る」は**どちらのモードでも四角固定**にする。押した瞬間にボタン自体が四角⇄角丸へ変形するのが気持ち悪いため（2026-08-01 ユーザー指摘）。角丸になるのはカテゴリバッジ（**両モードともピル**）・リスト表示の行（16px）・サムネ（12px）のみ
* 日付の等幅指定（`ui-monospace`）を廃止し、サイト共通の `var(--font-main)`（Inter + Noto Sans JP）＋ `tabular-nums` に統一。`/category/ニュース/` の `.m3-headline-card__date` も同様

### 15.5 残課題

* カテゴリバッジの文字色は `node_get_category_label_props()` が**常に白固定**のため、既定色 `#FF9900` 上ではコントラスト比 2.1:1 で WCAG AA（4.5:1）未達。カルーセル固有ではなくサイト全体のバッジ共通仕様なので、変更は全カード・全アーカイブに波及する。1.3 では現状維持とし、別タスクで判断する。

---

## 16. `/headlines/` の廃止（ニュースカテゴリへ301）✅ 完了（2026-08-01）

**判断**: `/headlines/` は「ニュースカテゴリを `cat=` で引き直すだけ」の単一カテゴリのラッパーで、同じ記事集合を出す `/category/ニュース/` と重複していた。専用URLを持つ価値がないので廃止する（2026-08-01 ユーザー判断）。

`/spotlight/` を残すのは非対称ではない。SPOTLIGHT は**複数カテゴリを束ねる**ため標準アーカイブに代替物がないが、HEADLINE は単一カテゴリの別名にすぎない。

### 16.1 廃止前の実測（廃止を後押しした事実）

| | `/headlines/` | `/category/ニュース/` |
| --- | --- | --- |
| `<title>` | 「Luminous Core – ガジェット…」（**サイト名のまま**） | 「ニュース – Luminous Core」 |
| canonical | なし | あり |
| wp-sitemap | 載らない | 載る |
| 1ページ件数 | 24（手書き） | 24（`node_custom_posts_per_page` が自動で） |

### 16.2 変更

* 撤去: `%node_headlines%` リライトタグ / `^headlines/?$` と `^headlines/page/N/?$` の2ルール / `query_vars` / `template_include` 分岐 / `node_headlines_pre_get_posts` / `template-parts/headlines.php`
* 追加: `node_redirect_legacy_headlines_url()`（`template_redirect` 優先度0）— `/headlines/`・`/headlines/page/N/` をクエリ文字列ごと301。既存リンクとクローラのために転送だけ残す
* `node_get_headlines_url()` はニュースカテゴリのURLを返すよう変更（カルーセルの「すべて見る」が自動的に追従）
* リライト版数 `node_all_articles_v5` → `v6`（旧ルールのフラッシュ用）

### 16.3 副次的に見つけて直したもの

* **`category-news.php` が使われていなかった**。カテゴリのスラッグが日本語（`ニュース`）のため `category-news.php` がテンプレート階層に一致せず、汎用 `archive.php` にフォールバックしていた。`category_template_hierarchy` フィルタ（`node_news_category_template_hierarchy()`）で専用テンプレートを先頭に差し込み、301の転送先が HEADLINE 専用デザインになるようにした。
* `.m3-headline-card__category` が `inline-flex` + `justify-content: center` だったため、長いカテゴリ名がピルの左右にはみ出して両端で切れていた（ellipsis も効かない）。`display: block` + `text-align: center` に変更。
* `.m3-headline-card__title` の1行クランプは和文タイトルがほぼ全て「…」で切れていたため2行へ。

### 16.4 回帰

`bun run verify:routes` を 11/11 green に更新（`/headlines/` の期待値を 200 → 301）。`composer test` 291件 green。

---

## 17. X投稿文言テンプレートの複数化（node-connect 1.3.7）✅ 完了（2026-08-08）

§3 の X 自動投稿は**ブログ全体で1本の固定テンプレート**だった。速報・レビュー・お知らせで同じ言い回しになるため、**テンプレートを複数登録し、記事ごとに選ぶ**方式へ拡張した。

### 17.1 データ構造

| | 旧 | 新 |
| --- | --- | --- |
| サイト設定 | `node_x_post_template`（文字列1本） | `node_x_post_templates`（`id => {name, content}` の配列） |
| 記事メタ | `_node_connect_x_text`（個別文面） | ＋ `_node_connect_x_template`（選択したテンプレートID） |

* **旧オプションは消さない**。`get_templates()` は新形式が未保存なら旧 `node_x_post_template`（空なら `DEFAULT_TEMPLATE`）を `default` キーの1件として返す。既存サイトは設定を触らなくても従来どおり動く
* `sanitize_templates()` は本文が空の行を捨て、IDを `[a-zA-Z0-9_-]` に正規化し、重複IDは連番へ退避する。全件消した場合は上記フォールバックで既定1件に戻る
* `sanitize_template_key()` は**存在しないIDを先頭テンプレートへ丸める**。テンプレートを削除しても、それを選んでいた記事が投稿不能にならない

### 17.2 UI

* **設定 → 外部連携**: テンプレート行（名前 + 本文）の追加・削除。行が1件のときは削除ボタンを `disabled`（0件にできない）。追加行のIDは `template-<timestamp>`
* **記事編集（ブロックエディタ）**: 右ペイン「X自動投稿」にテンプレート選択の `SelectControl` を追加
* **記事編集（クラシック）**: 同じ選択を `<select name="node_connect_x_template">` で提供

### 17.3 個別文面の上限を「重み付き280」から「140文字」へ

* 旧仕様は X の重み付き上限（`X_WEIGHTED_LIMIT = 280`、CJK=2 / ASCII=1 / URL=23）でそのまま切っていた。**書き手から見て何文字書けるのか分からない**ため、個別文面だけ `CUSTOM_TEXT_LIMIT = 140`（実文字数）へ変更した
* 超過分は `mb_substr` で切って `…` を付ける。エディタ側も入力時に 140 文字で丸め、カウンタ表示を「重み付き文字数」から素の文字数へ変更、`<textarea maxlength>` も付与
* **テンプレート経由の本文は従来どおり重み付き280に収める**（`{{summary}}` の切り詰め・タグの脱落）。140字制限は「記事ごとに手で書いた文面」だけに掛かる

### 17.4 テスト

`tests/node-connect-x-test.php` に ①記事ごとのテンプレート選択が反映されること ②不正なIDが先頭テンプレートへ戻ること ③個別文面が140文字へ丸められること を追加。

---

## 18. シェアボタンの「最小表示」トグル（テーマ本体）✅ 完了（2026-08-08）

記事下のシェアボタンはラベル付きの横長ボタンが並び、種類が増えるほど場所を取っていた。**ラベルを畳んで48pxの円形アイコンだけにする切り替え**を追加した。

* 見出し行（`.m3-share-heading`）の右端に `role="switch"` のトグルを置き、`aria-checked` で状態を持つ。`aria-controls` でボタン群 (`#m3-share-buttons`) を指す
* 状態は `localStorage`（`node-share-compact`）に保存し、次の記事でも維持する。localStorage が使えない環境（プライベートブラウズ等）では既定表示のまま動く
* **モバイル（<600px）ではトグル自体を出さない**。既存のモバイル用レイアウトが2列固定で、そこへ別の畳み方を持ち込む理由がないため
* `prefers-reduced-motion: reduce` でつまみのトランジションを無効化

---

## 19. 見出しフォントを Manrope へ分離（テーマ本体）✅ 完了（2026-08-08）

これまで見出しも本文も **Inter + Noto Sans JP** の同一指定だった。1.3 で **見出しだけ Manrope** に替え、本文は Inter のまま残す（2026-08-08 ユーザー決定）。

### 19.1 なぜ分けるのか

Manrope は字幅が広く終端が丸い幾何学系で、全大文字の見出し（`LUMINOUS CORE` / `HEADLINE` / `SPOTLIGHT`）や記事タイトルでは Inter より柔らかく開いた印象になり、和文（Noto Sans JP）と並べたときの**字面の大小差も目立ちにくい**。一方で本文・日付・文字数カウントのような読む量の多い箇所は、Inter の締まった字面のほうが読みやすい。**両方を1つの書体で賄わず、役割で分ける**。

### 19.2 実装

| ファイル | 変更 |
| --- | --- |
| `src/styles/_variables.css` | `--font-heading` を Manrope、`--font-main` を Inter に分離 |
| `header.php`（preload / 非同期 stylesheet / noscript の3か所） | Google Fonts を `Manrope:400,500,700,800` + `Inter:400,500,700` + `Noto Sans JP:400,500,700` の1リクエストに |

* 振り分けは既存の `src/styles/_reset.css` の `h1, h2, h3, h4, h5, h6, .m3-logo-text { font-family: var(--font-heading); }` がそのまま担う。**個別のコンポーネントCSSは触っていない**
* `_notice.css` の `var(--font-heading, "Manrope", …)` はフォールバックも見出し側なので追従済み
* **Manrope の最大ウェイトは 800**。テーマ内の `font-weight: 900` 指定は見出し側で 800 頭打ちになるが、Inter も 700 までしか読んでいなかったため実質の変化はない
* 実測（cybernode.local）: セクション見出し・カードタイトル = `Manrope`、日付・body = `Inter`、`document.fonts.check` で両方 loaded

---

## 20. トップページの表記と件数（2026-08-08 ユーザー指示）✅ 完了

* **HEADLINE の副題を「速報」→「ニュース」に変更**。表示先が `/category/ニュース/`（§16 で `/headlines/` を廃止して以降、実体はニュースカテゴリ）なので、**セクション名とカテゴリ名を一致**させる。トップの `template-parts/headline-carousel.php` と、301の転送先である `category-news.php`（アーカイブ見出し）の両方を変更した。カルーセルの `aria-label`（表示切替・前後ボタン・一覧）も同じ語に揃えている
* **LATEST の記事カードを四角に**（`_cards.css` で `.m3-surface--latest { --node-card-radius: 0 }`）。HEADLINE のカードと形をそろえる。カード枠とタイトルリンクの focus-visible 枠が同じ変数を見るので一緒に四角になり、アイキャッチも `overflow: hidden` でカード角に沿って切られる。**カテゴリバッジ・シリーズラベルは独自の radius を持つのでピルのまま**。実測: LATESTのカード6枚とも `border-radius: 0px`、バッジ 12px
* **LATEST の表示を 9件 → 6件**（`index.php` の `$latest_limit`）。3列 × 2段でちょうど割り切れるので、下端が欠けた行にならない。7件目以降は従来どおり「すべて見る」の下の通常グリッドへ流れるため、**トップから読める記事の総数は変わらない**（クエリも `node_custom_posts_per_page` のまま無変更）
* **トップの記事カードは全部四角**（2026-08-08 追加指示）。当初は LATEST 枠だけ四角にしたが、「すべて見る」より下の通常グリッドが角丸 28px のままで、スクロールすると四角と角丸が上下に並んだ。`.m3-surface--latest, .m3-surface--articles` の2クラスへ `--node-card-radius: 0` を当てて統一する。**この2クラスは `index.php` でしか使っていない**ので、アーカイブ・検索結果には波及しない（実測: 検索結果は 28px のまま、`/page/2/` は index.php なので 0px）
* **表示切替（カード ⇄ リスト）をピルボタンに**（2026-08-08 ユーザー指示）。外枠を丸い受け皿（`border-radius: 999px` + 3px パディング）にし、選択中のボタンだけを塗りの丸で示すセグメンテッドコントロールへ。カードやセクション枠は四角のままだが、**操作系はサイト共通のピル形状にそろえる**。両モードとも同じ形なので、押した瞬間に形が変わることはない（2026-08-01 の指摘を維持）
* **LATEST / 記事一覧のセクション枠も四角に**（2026-08-08 ユーザー指示）。`.m3-surface` の 40px 角丸を `.m3-surface--latest, .m3-surface--articles` で 0 に。HEADLINE の枠が四角なので、同じトップページに大きな角丸パネルと四角パネルが並ぶのを避ける。SPOTLIGHT（`.special-features.m3-surface`）は背景色が最も薄く枠が視認できないため 40px のまま残している
* **起動時のオレンジ背景を廃止**（2026-08-08 ユーザー指示）。ブランドオレンジ `#FF9900` のベタ塗りが3か所に残っていて、読み込み・起動のたびに画面がオレンジに光っていた。いずれもサイトのダーク面 `#1B1812` へ置換する。
  * `manifest.json` の `background_color` / `theme_color`（PWA起動画面とタイトルバー）
  * `header.php` の `<meta name="theme-color">`（ブラウザUI）。1本の固定値をやめ、`prefers-color-scheme` でライト `#FFF4E5` / ダーク `#1B1812` を出し分ける
  * **iOSの起動スプラッシュは廃止**（2026-08-08 ユーザー指示）。`header.php` の `apple-touch-startup-image`（10解像度）と `assets/pwa/splash-*.png` を削除した。ロゴを載せた全画面の起動画面は出るたびに一瞬挟まるだけで情報がなく、端末ごとに10枚を維持するコストにも見合わない。指定がなければ iOS は manifest の `background_color`（`#1B1812`）で塗るので、暗い無地から本体へ入る
  * **`apple-touch-icon` の背景もオレンジをやめて `#1B1812` に**（同日 追加指示）。ホーム画面で悪目立ちするため。透過のままにできないのは iOS が透過部を黒で塗るためで、無地にするなら黒よりサイトの面の色が合う
  * スクリプトは `scripts/generate-pwa-splash.mjs` → **`scripts/generate-pwa-icons.mjs`** へ改名（スプラッシュを作らなくなったため）
  * `pwa-icon-192/512.png` は背景が透過、`pwa-maskable-512.png` はクリーム `#FFF4E5` で、いずれもオレンジではないため今回は据え置き
