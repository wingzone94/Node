# DESIGN.MD — Node / Luminous Core デザイン基準

## 目的と位置づけ

NodeはLuminous Coreのコンテンツを読みやすく届けるWordPressテーマ。
本文の読みやすさ、情報の階層、迷わない操作、既存記事との互換性を優先する。
本書は今後のデザイン判断の共通基準であり、実装済み・本番検証済みを示す資料ではない。
作業範囲・モデル運用・検証・リリースは [AGENT.MD](./AGENT.MD) に従う。

[NODE-2.0.md](./NODE-2.0.md) のMaterial 3基盤と配色のガードレールを引き継ぐ。
同書の旧バージョン、試作名、日程、未実装Phaseを自動実行しない。
本書の追加だけで、親テーマ・子テーマへのデザイン変更を許可したことにはならない。

## ブランド

- サイト名は **Luminous Core**、親テーマ名は **Node**、テーマ作成者は **Luminous Core Teams**。
- Luna Frontier等の子テーマの名称と固有表現を親テーマ名で上書きしない。
- ブランドオレンジを既定のブランド色とする。既存ヘッダー・フッターの固定色を、記事由来の色で変更しない。
- ロゴ、固定ページURL、OGP上のブランド表記を装飾や整理の都合で変更しない。

## デザインシステム

- **Material 3を基盤**とし、Webの意味的HTML、既存CSSトークン、既存のバニラJS構成で実装する。
- Expressiveはブランド上の見せ場や有効な既存コンポーネント・モーションに限定する。全面適用や装飾だけのエフェクト追加を行わない。
- Android / Compose専用の仕様をそのままWebへ移さない。フレームワークやコンポーネント依存を文書整備に伴って導入しない。
- 色、文字、余白、形状、動きは既存の役割トークンを再利用する。新しい値を追加する前に既存の責務と適用範囲を確認する。

## 色とコントラスト

- 背景、本文、境界、リンク、操作、現在地の役割を区別する。本文は中立色を基本とする。
- 動的カラーを扱う場合はコンテンツ領域のsurface・accentを中心に適用し、画像なし・抽出失敗時も安定したフォールバックを用意する。
- カテゴリ固有色はカテゴリ識別のために保持し、サイト全体のprimaryへ流用しない。画像由来の色とカテゴリ固有色を混同しない。
- ブランドオレンジ等の塗りつぶしピル、バッジ、チップ、FAB、シリーズバナーの文字・アイコンは白を維持する。ユーザーの明示指示なしに黒・濃色へ変更しない。
- 白い文字のコントラストが不足する場合は、まず背景トーンなどで改善する。文字色を変更する案が必要なら対象と見た目を提示して確認する。`NODE-2.0.md` の「白固定をやめる」を既存コンポーネントへの無条件の変更許可と解釈しない。
- ライト・ダーク、hover、focus、選択状態で可読性を確認する。色だけで意味を伝えない。

## 文字と本文レイアウト

- 本文コンテナを自然に中央配置し、本文テキストは原則左揃えにする。本文全体へ中央揃えを適用しない。
- 見出し、本文、補助情報の階層を文字サイズ・行間・余白で明確にする。既存フォントの読み込みを重複させない。
- WordPressの `.has-text-align-*`、`.alignleft`、`.alignright`、`.aligncenter`、`.alignwide`、`.alignfull` を尊重する。
- 画像、figure、埋め込み、テーブル、ブログカード、目次、関連記事をコンテナに収める。長いタイトル・URL・日付でページ全体が横にはみ出さないようにする。
- 画像の余白問題では、レイアウト余白と画像自体の余白・縦横比・トリミングを区別する。

## コンポーネントと操作

- カード、検索、ナビゲーション、目次、FABは役割と操作先を明確にし、重複操作やリンクの入れ子を避ける。
- キーボード操作、見えるフォーカス、フォームラベル、開閉状態の伝達を保つ。コメント入力欄の保護は `AGENT.MD` に従う。
- 主なタッチ操作は48×48px以上の操作領域を設計上の目安とし、画面幅に応じて配置する。
- 固定UIは本文・操作対象を覆わず、モバイルのsafe-areaやWordPress管理バーがある状態でも使えることを確認する。
- 機能を撤去するときは、表示だけでなく関連するマークアップ・イベント・CSSの依存を対象範囲内で確認する。

## モーションと性能

- 動きは状態変化の理解を助ける範囲に抑え、`prefers-reduced-motion` に対応する。
- JSやアニメーションの失敗で記事やカードが隠れたままにならないようにする。
- 画像・フォント・動的色の読み込みによるレイアウト移動やちらつきを抑える。表示上の不具合を固定待機時間やJSの後付け位置補正で隠さない。
- 機能は必要なページ・DOMで初期化し、不要な読み込みと二重イベント登録を避ける。

## 実装の境界

- 親テーマ共通機能と子テーマ固有表現を区別する。子テーマの依頼は、親への変更が必要と確認されるまで子の範囲に保つ。
- CSSは責務に合うpartialを修正し、親からの継承と実際のcomputed styleを確認する。
- `!important` は乱用しない。必要な例外は対象と理由を限定し、末尾へのパッチ積み重ねや全面整形を避ける。
- 新しいページ構成・ルート・サイト全面のデザイン変更は個別の依頼として扱う。

## 変更時の確認

- 対象、目的、変更範囲、既存記事への影響を短く記録する。
- テーマ変更では `AGENT.MD` のビルド→cybernode.local反映→ブラウザ検証を行う。親・子のアセットとmanifestが検証対象に一致することを確認する。
- スマートフォンとデスクトップで、通常記事、長いタイトル、画像なし、検索、アーカイブなど変更に関係する条件を確認する。
- 横はみ出し、固定UIの重なり、キーボード操作、文字の可読性、モーション軽減を確認する。配色変更では該当するライト・ダーク状態も確認する。
- スクリーンショットはチャットに画像として共有する。文書上の基準、コード確認、ローカル実測、本番確認を区別して報告する。
