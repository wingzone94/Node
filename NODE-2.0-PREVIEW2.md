# NODE 2.0 Preview 2 — リファクタリング案（デザイントークン欠落の是正）

> 本書は `NODE-2.0.md`（2.0 設計の正本）の**従属文書**であり、Preview 2 の作業単位だけを切り出したもの。
> `NODE-2.0.md` が定める配色思想（オレンジ＝フォールバック / ヘッダー・フッターは固定の額縁）、
> 2系統分離、リリース方針は本書では一切覆さない。
> AGENTS.md の制約（1タスク1目的・原則5ファイル・既存記事を壊さない・cybernode.local 検証）を厳守すること。

---

## 1. 発端

Discord `#ご意見・ご感想` に、トップページ（iPhone / Safari）のスクリーンショット付きで以下の指摘が挙がった。

```text
ダメなところ
うそのグラスモーフィズム
変なドロップシャドウ
デカすぎるピル型のタグ
色
カード
```

この5点が「趣味の問題」なのか「実装の欠陥」なのかを切り分けるため、出荷 CSS を実測した。

---

## 2. 結論

**5点中4点は趣味の問題ではない。**

根因はひとつ、**宣言されているのに定義されていないデザイントークン**である。
`box-shadow` と副文の色が広範囲で無効化されており、影・カード・グラス・色の見え方が
まとめて設計意図から外れている。つまり「デザインの好み」ではなく「設計どおりに描画されていない」。

指摘者はスクリーンショット1枚から、CSS 変数の欠落という単一の原因が生む症状群を正確に列挙している。

---

## 3. 計測方法（再現手順）

出荷物 `assets/css/style.css` と、`node_generate_m3_colors()`（`inc/utilities.php:332-365`）が
`wp_head` に出す `<style id="m3-dynamic-colors">` 相当の `:root` を重ねた DOM を、
Chromium（Playwright / viewport 390×844）で `getComputedStyle` 実測した。

**注意（誤診の罠）**: 出荷 CSS だけを見ると診断を誤る。
`--md-sys-color-surface-container-high` と `--md-sys-color-outline` の light 値は
`src/styles/` 側には存在せず、**ランタイムのインライン `<style>` 側にしかない**。
CSS ファイルだけで検証すると「light のヘッダー背景が透明」という実際には起きない結論に至る。
以降の数値はすべてインライン込みで測っている。

---

## 4. 実測結果

| 対象 | 実測値 | 設計意図 |
| --- | --- | --- |
| `.m3-card` の `box-shadow` | `none` | `var(--m3-elevation-1)` |
| `.m3-card:hover` の `box-shadow` | `none` | `var(--m3-elevation-3)` |
| `.m3-header` の `box-shadow` | `none` | `var(--m3-elevation-1)` |
| `.m3-spotlight-badge` | 高さ **45px** / 文字 **18.4px** / 影あり | — |
| 直上の `.m3-headlines__title` | 文字 **14.4px** | 見出し > タグ のはず |
| 副文（`--md-sys-color-on-surface-variant` 指定） | `rgb(43,23,0)` ＝ **本文と同色に継承** | 弱いトーン |
| `.m3-sticky-toc` | 背景 **98% 不透明** ＋ `blur(25px)` | ガラス |
| `.m3-toc-card` | 背景 **完全不透明** ＋ `blur(10px)` ＋ 影 `none` | ガラス＋浮き |

カードの `transition: box-shadow 0.4s` は、両端が `none` のため**死んだコード**になっている。

---

## 5. 未定義トークンの棚卸し

| トークン | 定義状況 | フォールバック無しの参照数 |
| --- | --- | --- |
| `--m3-elevation-1` 〜 `-4` | **どこにも未定義** | 36（1:16 / 2:12 / 3:3 / 4:5） |
| `--md-sys-elevation-level1` | **どこにも未定義** | 1 |
| `--md-sys-color-on-surface-variant` | **どこにも未定義** | 94 |
| `--md-sys-color-surface-container-highest` | dark と print のみ（**light 欠落**） | 35 |
| `--md-sys-color-surface-container-high` | `inc/utilities.php` のインラインでのみ light 定義 | 78 |
| `--md-sys-color-outline` | 同上 | 27 |

未定義変数を参照した宣言は「invalid at computed-value time」となり、
`background` は初期値（透明）に、`color` は**親から継承**する。
後者のせいで、弱く見せるはずの副文・日付・抜粋が本文と同じ濃さで出ている。

`box-shadow` 宣言は全 **222** 個。うち**トークン経由が 37（＝全滅）**、
残り **185 が一点物の手打ち**で、`0 40px 100px rgba(0,0,0,.3)` のような極端値も混在している。

### 二重パレット問題

`src/styles/_variables.css` の dark surface 値は、`wp_head` のインラインに後勝ちで上書きされ**死んでいる**。

| トークン | `_variables.css` | インライン（実際に効く方） |
| --- | --- | --- |
| `--md-sys-color-surface` (dark) | `#1B1812` | `#1e1b16` |
| `--md-sys-color-surface-container-low` (dark) | `#221F18` | `#25221b` |
| `--md-sys-color-surface-container` (dark) | `#2A261F` | `#2a2720` |
| `--md-sys-color-surface-container-high` (dark) | `#322E26` | `#322f28` |

開発者が編集する側のファイルが効かない状態であり、色の調整が「効かない」と感じる原因になる。

---

## 6. 指摘 → 原因の対応

| 指摘 | 判定 | 原因 |
| --- | --- | --- |
| うそのグラスモーフィズム | **正しい** | `backdrop-filter` 11 ルール中、実効あり4・ほぼ無効4・完全無効3 |
| 変なドロップシャドウ | **正しい** | elevation 37 宣言が全無効 → カードは影ゼロ、ピルだけ影あり＝**階層が逆**。`.m3-sticky-toc` は実体のない影だけが出る |
| デカすぎるピル型のタグ | **正しい** | 45px / 18.4px / アイコン 24px 固定。M3 chip は 32dp / 14sp / 18dp |
| 色 | **半分正しい** | オレンジ自体ではなく使い方。副文が本文色に継承されて階層が消える（下記コントラストも参照） |
| カード | **正しい** | 上2つの複合。影ゼロ ＋ ほぼ見えない罫線 ＋ 副文が本文と同色 → 境界も階層もない板が並ぶ |

### コントラスト実測（WCAG）

| 組み合わせ | 比 | AA (4.5) |
| --- | --- | --- |
| 白文字 on ブランドオレンジ `#FF9900` | **2.14:1** | ✗ |
| オレンジ見出し `#FF9900` on 白カード | **2.14:1** | ✗ |
| オレンジ見出し `#FF9900` on クリーム地 `#FFF4E5` | **1.97:1** | ✗ |
| 罫線 `#d6c2b1` on 白カード | **1.72:1** | （非文字だが実質不可視） |
| 既存の `--md-sys-color-accent-text: #935400` on 白カード | 5.99:1 | ✓ |
| 本文 `#2b1700` on クリーム地 | 15.77:1 | ✓ |

**AA を満たす `#935400` を既にトークンとして持っているのに、見出しで使っていない。**

### `backdrop-filter` の仕分け（全11ルール）

- **実効あり（4）**: `_blogcard-ai.css:451`（黒0.7 / 画像上）、`_archive.css:184`（55%）、
  `_adblock-modal.css:6`（黒0.4）、`_header.css:686`（`::backdrop` 0.6）
- **ほぼ無効（4）**: `_loading.css:7`（0.85）、`_header.css:1213`（0.9）、
  `_header.css:64`（0.94）、`_article.css:2084`（0.95）
- **完全無効（3）**: `_fab.css:61`（0.98）、`_toc-sidebar.css:59`（**完全不透明**）、
  `_article.css:711`（単色の親の上に乗ったピル ＝ ぼかす対象がない）

`-webkit-backdrop-filter` を併記しているのは 11 ルール中 **2 つだけ**（`_adblock-modal.css` / `_fab.css`）。
Safari は 18.0 で無接頭辞に対応したため、それ以前の iOS では残り 9 ルールが効かない。

---

## 7. リファクタリング案

AGENTS.md「1タスク1目的」に従い、以下を**別タスクとして**分割実施する。

### R1 — elevation トークンの定義

- **対象**: `src/styles/_variables.css`（1ファイル）
- **内容**: M3 標準の黒ベース2段重ねで定義する（トーンはユーザー選択済み）。

```css
--m3-elevation-1: 0 1px 2px 0 rgba(0,0,0,.30), 0 1px 3px 1px rgba(0,0,0,.15);
--m3-elevation-2: 0 1px 2px 0 rgba(0,0,0,.30), 0 2px 6px 2px rgba(0,0,0,.15);
--m3-elevation-3: 0 4px 8px 3px rgba(0,0,0,.15), 0 1px 3px 0 rgba(0,0,0,.30);
--m3-elevation-4: 0 6px 10px 4px rgba(0,0,0,.15), 0 2px 3px 0 rgba(0,0,0,.30);
--md-sys-elevation-level1: var(--m3-elevation-1);
```

- **受け入れ条件**: カードの hover で影が変化する（`transition: box-shadow 0.4s` が生きる）／
  ヘッダーが本文から浮く／`.m3-toc-card` に影が出る。
- **リスク**: 37 箇所が一斉に効くため、light の見た目変化が本書中で最大。
  **Preview 2 で最初に、単独で実施すること。**

### R2 — 欠落カラートークンの定義と正本の一本化

- **対象**: `src/styles/_variables.css` / `inc/utilities.php`
- **内容**:
  - `--md-sys-color-on-surface-variant` を light / dark 両方に定義。
    案: light `#6B5A4A`（白カード上 6.59:1 / クリーム地 6.06:1）、dark `#D6C6B8`。
    本文 `#2b1700`（15.77:1）と十分な差があり、かつ AA を満たす。
  - light の `--md-sys-color-surface-container-highest` を定義（現状 35 箇所が透明落ち）。
  - surface 系の正本を CSS 側／インライン側のどちらかに寄せ、§5 の二重定義を解消する。
- **受け入れ条件**: 94 + 35 箇所の副文・hover 背景が意図どおり出る／dark の見た目が現状から変わらない。

### R3 — 偽グラスモーフィズムの整理

- **対象**: `_toc-sidebar.css` / `_fab.css` / `_header.css` / `_article.css` / `_loading.css`
- **内容**: §6 の仕分けに従い、
  - 「完全無効（3）」は `backdrop-filter` を削除（GPU コストだけ払って効果がゼロのため）
  - 「ほぼ無効（4）」は透過を上げて本当にガラスにするか、削除するかを **1件ずつ**決める
  - 残す全ルールに `-webkit-backdrop-filter` を併記
- **受け入れ条件**: 残った `backdrop-filter` はすべて背景透過が実効ある値であること。

### R4 — SPOTLIGHT ピルの縮小

- **対象**: `src/styles/_article.css:795`（`.m3-spotlight-badge`）、
  `src/styles/_spotlight.css:14`（アイコン枠）、`:297`（モバイル上書き）
- **内容**: 文字 `1.15rem` → `0.9rem` 前後、アイコン `24px` 固定 → `18px`、高さ 45px → 36px 相当。
  角丸はピルのまま維持する（`NODE-2.0.md` の意匠を変えない）。
- **受け入れ条件**: セクション見出し（14.4px）より小さいこと／
  モバイルのタップ領域が 44px を下回らないこと（padding か擬似要素で確保）。

### R5 — 手打ち影 185 箇所のトークン化（**Preview 3 以降へ送る**）

R1 の定義に寄せて段階的に置換する。範囲が広いため、
AGENTS.md「CSS 整理は 1 回の作業では対象を限定する」に従い partial 単位で分割すること。
本書では実施しない。

### R6 — コントラスト（**提案のみ・実装は別途承認**）

§6 の数値のとおり、白 on `#FF9900` と オレンジ見出し on 白カードが 2.14:1 で AA を大きく下回る。

ただし AGENTS.md には
「ブランドオレンジ等の塗りつぶしピル、バッジ、チップ、FAB、シリーズバナーは文字・アイコンを白で維持する」
「コントラスト改善が必要でも、ユーザーの明示指示なしに黒や濃色へ変更しない」
と明記されているため、**本書では数値の提示にとどめ、変更は行わない。**

なお `NODE-2.0.md` §1 の差分表は現状を「文字白固定＋影で誤魔化し」と課題視し、
§2.4 は「on-color は背景トーンに対し WCAG AA を満たすトーンを選択（白固定をやめる）」と既に定めている。
**この制約は 1.x 系のものであり、2.0 の動的カラー導入時に解禁される見込み**である。
R6 を単独で急ぐより、2.0 のトーナルパレット導入とまとめて扱うほうが整合する。

---

## 8. 実施順とスコープ線引き

```text
R1 → R4 → R2 → R3     （効果が大きく影響範囲が狭い順）
R5 → Preview 3 送り
R6 → 要承認 / 2.0 のトーナルパレットと合流
```

`NODE-2.0.md` §7 の「スリップしそうなら日付ではなく範囲を動かす」に沿い、
**Preview 2 は R1 と R4 だけでも成立する**。R2・R3 は Preview 3 へ送ってよい。

---

## 9. 未確定事項

- surface 系トークンの正本を CSS 側／インライン側のどちらに寄せるか（R2 で決める）
- 「ほぼ無効」4件を透過させるか削除するか（R3 で 1 件ずつ判断）
- ピルの最終寸法（R4 の実装時に cybernode.local と実機で確認）

---

*本書は Discord の指摘を起点とした調査結果と是正案であり、実装は含まない。
`NODE-2.0.md` の設計判断と矛盾する変更を本書を根拠に行わないこと。*
