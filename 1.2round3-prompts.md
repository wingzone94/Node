# Node v1.2 Round 3（ボトムバー排除 / ヒーロー縦分割＋目次統合＋サムネ見切れ解消 / ライブラリアーカイブ＋ゲーム個別ページ / ライター別アーカイブ導線 / 日付アーカイブ新設計） — 実装用プロンプト

[1.2round2-prompts.md](1.2round2-prompts.md) に続く v1.2 Round 3 の実装プロンプト。設計判断は本ファイルに確定済みのため、各プロンプトをコピーしてそのまま該当エージェントに渡すこと。

役割分担（[[node-llm-lineup-policy]]準拠、ただし2026-07-09にGPT-5.5の使用制限を受けてユーザー判断でスポット変更）: **①= Codex着手→Fable 5（Claude Code）が引き継ぎ完了**。**②⑤⑥= Opus 4.8 / Fable 5（Claude Code）**（新規デザイン・レイアウト・配色・レスポンシブ・ダークモードなど視覚判断が実装の中心のため。Round 2のお知らせブロックと同じ基準）。**③= Gemini 3.1 Pro（High、AntiGravity）**（GPT-5.5制限回避のためのスポット起用。恒常的な編成変更ではない。基盤実装として完了、デザインは⑤で新規に起こす）。**④= Codex（制限中ならFable 5）**。検証・リリースゲート = Claude Code。**各タスクは必ず別タスク（別セッション）として1つずつ実行すること（1タスク1目的）。**

推奨実行順: ①③は完了済み。残りは ④ライター導線（小） → ②ヒーロー縦分割＋目次統合 → ⑤ライブラリ新設計 → ⑥日付アーカイブ新設計（②⑤⑥はOpus/Fable 5レーンのため必ず別セッションで1つずつ）。**①実行後〜②完了までの間、モバイルの目次導線が一時的に消える**（②のヒーロー統合目次で復活する）。①②はセットでリリースゲートに乗せ、片方だけのリリースはしないこと。

## 確定済みの設計判断（2026-07-07 ユーザー確認済み）

- **ヒーロー右カラムのデータソース**: 新規メタ・AI生成は導入しない。**既存要素（カテゴリラベル・AI開示バッジ・スポンサーバッジ・読了バッジ・メタ情報・目次）の再配置のみ**で構成する。モックアップにある「訴求ヘッダー（大きなキャッチ）」「関連情報リスト（アイコン＋短文×3）」は**今回実装しない**（データが存在しないため。将来ラウンド候補）。
- **サムネイル**: ヒーロー上部の全幅表示を維持しつつ、**記事ごとに倍率を自動調整して画像全体が写る**ようにする（現行の `min-height` + `object-fit: cover` による強制切り抜きを廃止）。見切れ問題の修正は②に統合。
- **モバイル目次導線（2026-07-07 更新）**: モバイルの目次は**記事タイトルカード（ヒーロー）に統合したプルダウン**とし、ヒーロー通過後は**現在セクションを表示する追従バー**（タップでプルダウン展開、選択でジャンプ）が引き継ぐ。目次データは `node_get_article_toc_items()`（inc/toc-engine.php）からサーバーサイドで取得（新規データ導入なしの原則に合致）。
- **目次はページ単位**: マルチページ記事（`<!--nextpage-->`）では、モバイルの目次項目をページ単位で扱う。現在ページ内の見出しはスクロールジャンプ、別ページの見出しはページ区切りでグループ化して表示し、選択時は該当ページURLへ遷移（既存 `node_get_multipage_url()` を使用）。追従バーの現在地表示は現在ページ内の見出しに同期する。
- **モバイルFABはトップのみ**: 目次FAB（#m3-toc-trigger）はモバイルでは表示しない（ヒーロー統合目次＋追従バーに一本化）。モバイルFABは「トップへ戻る」のみ。PCは現行どおり（FAB＋追従目次パネル）変更しない。
- **アーカイブ（2026-07-09 更新・ユーザー確認済み）**: 日付アーカイブ・ライブラリアーカイブとも**カテゴリアーカイブ（m3-archive-header＋カード列）の流用はしない。完全新規の設計**とする。
  - ライブラリ一覧（/node-library/）: **ストアフロント風**（ゲーム/アプリのタイプフィルター等を備えたショーケース型）に新規デザイン（⑤）。
  - ライブラリ個別ページ（/node-library/{slug}/）: **ゲームごとの個別ページを完全新規の設計で正式実装**し、記事内のライブラリカードからもアクセス可能にする（⑤）。③の「薄い単体ページ」論点はこれで解決（リダイレクト・noindex不要）。
  - 日付アーカイブ: 新規デザインで実装（⑥）。デザインの方向性は実装担当（Opus/Fable 5）に一任。

---

## ① モバイルボトムバー排除＋トップFABのみモバイル表示 — 実装（Codex / GPT-5.5）— **実装完了**

> **実装メモ（2026-07-09）**: Codexが撤去作業をほぼ完了した時点で使用制限に到達し、Fable 5（Claude Code）が引き継いで完了。追加修正は `_fab.css` に残っていたボトムバー前提の `bottom: 100px` → `24px` の1件のみ。残参照ゼロ・構文チェック・vite build確認済み。cybernode.local実機確認はリリースゲートで実施。

```
Node テーマ（WordPress、/Users/saitoutatsuya/Documents/GitHub/Node）のv1.2向けに、モバイルのボトムバー（ハンディナビゲーション）を撤去し、「トップへ戻る」FABのみをモバイルでも表示する変更を実装してください。一人で完走すること。

# 背景
記事ページのモバイル表示（1000px以下）で画面下部に固定表示されるボトムバー（.m3-bottom-nav、目次/トップの2ボタン）を廃止する。トップへの導線は、現在PC専用（min-width: 1001px）になっているFABスタック（.m3-action-stack）のうち #m3-back-to-top のみをモバイルにも表示して引き継ぐ。**目次FAB（#m3-toc-trigger）はモバイルでは表示しない**（モバイルの目次導線は別タスク②で「ヒーロー統合プルダウン＋追従バー」として実装されるため。本タスク完了時点でモバイルの目次導線が一時的に無くなるのは想定内であり、対応不要）。

# 撤去対象（実装前に必ずgrepで参照を洗い出すこと）
1. template-parts/components/bottom-bar.php — ファイル削除。footer.php:47 の get_template_part 呼び出しも削除。
2. src/styles/_navigation.css の .m3-bottom-nav 一式（492行付近〜、.m3-bottom-nav__item / __label / モーダル時非表示 / @media (max-width:1000px) の display:flex / .is-handy-mode .m3-bottom-nav を含む）。
3. src/styles/_navigation.css:576付近 — ボトムバー前提の .site-main { padding-bottom: calc(100px + env(safe-area-inset-bottom)) } 補正。FABと本文が重ならない程度の妥当な値に縮小（例: 24px程度、実表示で判断）。
4. src/styles/_navigation.css の @media (max-width:600px) にある .m3-sticky-navigation / .m3-floating-toc-card の bottom オフセット（ボトムバーの高さ100px/92pxを前提にした値）— バー撤去後の位置に調整。
5. src/styles/_bottom-bar.css — ファイル削除＋ src/styles/style.css:29 の @import 削除。（.m3-bottom-bar はテンプレートのどこからも参照されていない死んだCSS。body.scroll-down/.scroll-up 連動ルールも同ファイル内のみなら一緒に消える）
6. src/scripts/expressive-toc.js — handyTrigger（#m3-handy-toc-trigger）と bottomNav（#m3-bottom-nav）関連の生成・バインド・表示切替コードを除去。#m3-toc-trigger（FAB）経由の目次開閉は絶対に壊さないこと。
7. src/scripts/handy-mode.js — #m3-back-to-top-handy はボトムバー専用なので除去。#m3-bottom-comments-trigger は現存テンプレートに出力箇所があるかgrepで確認し、なければファイルごと削除＋ src/main.js:8 の import/呼び出しを削除。残す要素があればその分だけ残す。
8. .is-handy-mode / handy 関連のクラス・参照が他に残っていないか最終grepで確認。

# トップFABのモバイル表示
- src/styles/_toc-fab.css:8 の @media (min-width: 1001px) で .m3-action-stack.is-singular .m3-fab を表示している箇所を調査し、モバイル（1000px以下）では **#m3-back-to-top のみ**表示されるように変更。#m3-toc-trigger はモバイルでは非表示のままにする（display等で明示的に切る）。
- モバイルでのサイズ・位置: 既存のFABサイズ（56px系）を維持し、右下配置。env(safe-area-inset-bottom) を考慮。
- PC側の表示・挙動（FAB2つ＋追従目次パネル、body.is-active-toc 時の退避挙動 _toc-fab.css:22）は一切変更しない。

# 制約（AGENTS.md準拠）
- 全体リライト禁止。目次パネル本体・PC表示の挙動は変更しない。
- CSS/JS変更後は bun x vite build を必ず実行し、assets/ と manifest を最新化。
- node --check（対象JS）で構文チェック。php -l（footer.php）。

# 完了条件
- モバイル幅（〜1000px）の記事ページ: ボトムバーが存在せず、トップFABのみ表示され、トップスクロールが動作する。目次FABは表示されない
- 本文下部に不自然な余白が残らない／FABが本文・フッターと重ならない
- PC幅（1001px〜）の表示・挙動（目次FAB・追従目次パネル含む）が一切変わっていない
- 固定ページ・フロントページ等、is_singular以外でレイアウト崩れがない（ボトムバーはis_singular限定だったため影響確認のみ）

# 報告
変更・削除ファイル一覧、ビルド結果、cybernode.localでの確認項目（モバイル/PC各1記事、URL付き）を報告すること。
```

---

## ② 記事タイトルカード（ヒーロー）縦分割再構成＋目次統合＋サムネ見切れ解消 — 実装（Opus 4.8 / Claude Code）— **実装完了（ゲート検証待ち）**

> **実装メモ（2026-07-09確認）**: 別セッション（Opusレーン）で実装済みであることをコード確認。hero.php の2カラム構成（.m3-hero-col--aside）、ネイティブselectベースの目次プルダウン（node_get_article_toc_export_items使用・マルチページのページ単位グループ化対応）、src/scripts/hero-toc.js（IntersectionObserverによる追従バー）、_hero-toc.css、サムネ見切れ解消（min-height撤廃＋縦長はcontain上限、_article.css:814付近）まで揃っている。実機確認はリリースゲートで実施。

```
Node テーマ（WordPress、/Users/saitoutatsuya/Documents/GitHub/Node）のv1.2向けに、single.phpの記事タイトルカード（ヒーロー、template-parts/single/hero.php）をPC縦2カラム構成に再構成し、モバイル向けのヒーロー統合目次（プルダウン＋追従バー）を実装し、あわせてサムネイルの見切れ問題を解消してください。このタスクは最初から最後まで一人で完走すること。カラム配分・区切り線の質感・余白・ダークモード整合・追従バーの佇まいはあなたの判断で品質を確保すること（このタスクがOpus担当なのはその判断を任せるため）。

# 機能概要（ユーザー提供のGPT-Image実装イメージに基づく。確定済み設計判断）
- 縦ライン（PC）で「本文情報」と「補足・訴求情報」を分離: 左=記事の核心（カテゴリ・タイトル・メタ）／右=補足・訴求（バッジ類・目次）。
- 横ラインで情報の階層を整理: 上=カテゴリ・ラベル／中=タイトル／下=メタ情報（日付・追記・コメント・読了時間）。
- **新規データは導入しない。** モックにある「訴求ヘッダー（7/1再開のような大きなキャッチ）」「関連情報リスト（アイコン＋短文×3）」は実装しない。右カラムは既存要素（AI開示バッジ・スポンサーバッジ・読了バッジ・目次等）の再配置のみで構成する。
- 読了時間・文字数バッジは従来のトーン（m3-article__reading-badge-expressive）をそのまま活かす。
- モバイルの目次はヒーローに統合したプルダウンとし、ヒーロー通過後は現在セクションを示す追従バーが引き継ぐ（下記「ヒーロー統合目次」参照）。目次FABはモバイル非表示（別タスク①で対応済み）。

# 確定仕様
1. 対象: template-parts/single/hero.php と CSS（src/styles/_article.css / _article-polish.css、必要なら新規 _hero-split.css を作成し style.css に @import 追加）。
2. レイアウト:
   - PC（テーマ既存のブレークポイント体系に合わせ 1001px以上。モック記載の1024pxではなく既存の1000px境界に統一）: 縦2カラム。左カラム=カテゴリラベル → タイトル → メタ情報（日付・追記・コメント）。右カラム=AI開示バッジ・スポンサーバッジ・読了バッジなど補足・訴求要素。
   - タブレット/モバイル（1000px以下）: 現行同様の1カラムに自動切り替え（要素順は左カラム→右カラムの内容が自然に続く順）。
   - 縦ライン: border-right: 1px dashed（区切り色は --md-sys-color-outline-variant 基調、実色は判断に任せる）。横ライン: border-bottom: 1px dashed 同系。
   - 各エリアは余白（padding）で視覚的に分離。色味は既存のオレンジトーン（Material 3トークン）を維持。アイコンは既存どおり Material Symbols。
3. ヒーロー統合目次（モバイル中心。このタスクの新規実装部分）:
   - データ: inc/toc-engine.php の node_get_article_toc_items()（マルチページ対応済み、各itemはcurrentフラグとページ跨ぎURLを持つ。node_get_multipage_url() 参照）からサーバーサイドで出力。目次項目が0件の記事では目次UIを出さない。
   - ヒーロー内プルダウン: hero.php（モバイル時はメタ情報の下あたり、PC時は右カラム）に目次プルダウンを設置。実装はネイティブ <select> ベースを推奨（OSピッカーが使え、タップ操作性とa11yが堅い。閉状態の見た目だけM3トーンでスタイリング）。カスタムUIにする場合もキーボード・スクリーンリーダー対応を落とさないこと。
   - ジャンプ挙動（ページ単位ルール）: 現在ページ内の見出し選択 → スムーズスクロールでジャンプ。マルチページ記事の別ページ見出し選択 → 該当ページURLへ遷移。プルダウン内では「ページ2」等の区切り（<optgroup>等）でページ単位にグループ化する。単一ページ記事では見出しをフラットに列挙。
   - 追従バー（モバイルのみ）: ヒーローがビューポートから出たら、画面上部に現在セクション名を表示するコンパクトなバーを表示（IntersectionObserver等）。タップでプルダウン展開（selectなら同じselectを内包）、選択でジャンプ。スクロールに応じて現在セクション表示を同期する（expressive-toc.js に既にある現在見出し判定ロジックを流用・共通化してよい）。ヘッダーやモーダルと z-index/位置が衝突しないこと。
   - PC: 既存の追従目次パネル（#m3-sticky-toc）とFABは一切変更しない。PCヒーロー右カラムに目次を載せる場合は、追従パネルと役割が重複しすぎない範囲（「読む前の全体把握」用の静的リスト or プルダウン）であなたが判断する。
   - JS追加は src/scripts/ 配下に新規ファイル（例: hero-toc.js）または expressive-toc.js への最小追加とし、既存の目次パネル開閉・追従ハイライトを壊さないこと。
4. サムネイル見切れ解消（このタスクに統合）:
   - サムネはヒーロー上部の全幅表示を維持。
   - 現行の強制切り抜きの原因: src/styles/_article.css:807-815 の .m3-article__featured-img { min-height: clamp(200px, 40vw, 450px); object-fit: cover } と、.m3-article__featured-image:768 の min-height。
   - これを「記事ごとに倍率を自動調整して画像全体が写る」挙動に変更する。基本方針: min-height撤廃＋height:auto の自然アスペクト比表示とし、極端に縦長/横長の画像でヒーローが破綻しないよう max-height 上限（およびその際の収め方: contain＋背景色 等）はあなたが判断する。PC・モバイル両方で見切れが解消されること。
   - _article-polish.css のオーバーラップ構成（.m3-article__header--overlap の負マージン、featured-gradient非表示等）は新レイアウトに合わせて整理してよいが、has-no-thumbnail 記事の装飾（_article-polish.css:73〜）は破綻させないこと。
5. フォールバック/回帰:
   - サムネ無し記事（has-no-thumbnail）: 2カラム構成は適用してよいが、既存の「ヒーロー感」装飾を後退させない。
   - 読了バッジのクリック展開（expressive-toc.js等のJS連携、#m3-hero-reading-badge / #m3-hero-comment-trigger のID）を維持。IDやaria属性を変えないこと。
   - 構造化データ・OGP（inc/seo.php）は触らない。

# 制約（AGENTS.md準拠）
- 変更は hero.php + CSS 3ファイル程度 + JS 1ファイル（新規 or expressive-toc.jsへの最小追加）まで。
- ビルドは bun x vite build のみ（npm/yarn/pnpm禁止）。CSS/JS変更後に必ず実行。
- php -l / node --check で構文チェック。
- ライト/ダークモード両対応を必ず確認。

# 完了条件
- PC: ヒーローが縦2カラム＋横ラインの階層構成になり、破線の区切りで情報が整理されて見える
- サムネイルが PC/モバイルとも全体表示され、見切れが発生しない（横長・縦長・正方形の3パターンで確認）
- モバイル: 1カラムに自然に折りたたまれ、ヒーロー内プルダウンから各見出しへジャンプできる
- モバイル: ヒーロー通過後に追従バーが現れ、現在セクション名がスクロールに同期し、タップ→選択でジャンプできる
- マルチページ記事: プルダウンがページ単位にグループ化され、別ページ見出しの選択でそのページへ遷移する
- 目次項目0件の記事では目次UIが出ず、レイアウトが破綻しない
- PCの既存FAB・追従目次パネルの挙動が一切変わっていない
- サムネ無し記事・AIバッジ無し記事・コメント0記事でレイアウトが破綻しない
- ダークモードで配色が破綻しない

# 報告
変更ファイル一覧、ビルド結果、cybernode.localでの確認項目（サムネ有り/無し/横長/縦長、マルチページ記事、見出し無し記事の記事URL付き）を報告すること。
```

---

## ③ ライブラリアーカイブページ新設 — 実装（Gemini 3.1 Pro High / AntiGravity）— **実装完了・実機確認済み**

> **担当変更メモ（2026-07-09）**: 当初Codex（GPT-5.5）担当だったが、使用制限回避のためユーザー判断でGemini 3.1 Pro（High）にスポット変更。恒常的な編成変更ではない（[[node-llm-lineup-policy]]の例外運用）。
>
> **検証メモ（2026-07-09、Claude Code実機確認）**: cybernode.localで /node-library/ が200・カードグリッド30件・ページネーション・アーカイブヘッダー（ライブラリ/menu_book/件数）・パンくず全て動作。バージョン比較flush（node_library_flushed_version）も自動発火し、パーマリンク再保存は不要だった。~~**未解決の論点1件**: has_archive成立のため `publicly_queryable => true` になった副作用で、ライブラリ単体URLが薄いページとして公開される件~~ → **解決（2026-07-09）**: ユーザー判断により、⑤でゲームごとの個別ページを完全新規デザインで正式実装する（リダイレクト・noindex不要）。また一覧・個別ともカテゴリアーカイブ流用をやめ新規設計とするため、③はリライトルール・has_archive・flush等の**基盤実装**として位置づけ、見た目は⑤で全面的に作り直す。

```
あなたはWordPressテーマ「Node」（/Users/saitoutatsuya/Documents/GitHub/Node）の開発を担当します。v1.2向けに、node-libraryプラグインのライブラリアーカイブページ（/node-library/ 一覧）を新設してください。このタスクは調査→実装→検証→報告まで、あなた一人で完走してください。

# 最初にやること（実装前に必須）
1. リポジトリ直下の AGENTS.md を読み、開発規約（ビルドコマンド・変更範囲の制限・動作確認チェックリスト）を把握する。
2. plugins-embedded/node-library/node-library.php を読み、次を調査してから設計を決める:
   - register_post_type の定義（754行付近。現在 has_archive => false、rewrite slug 'node-library'）
   - テンプレートの供給方法（single表示やカード表示を template_include フィルタで供給しているか、テーマ側テンプレートに任せているか）
   - activationフック・バージョン定数・DBオプションによる初期化機構の有無（リライトルールflushの発火点に使うため）
3. plugins-embedded/node-library/templates/card-library.php（既存のライブラリカード）と、テーマの inc/archive-helpers.php node_get_archive_context() / archive.php を読む。
調査結果と実装方針を先に短くまとめてから、コードを書き始めること。

# 背景
カスタム投稿タイプ node_library にはアーカイブページが存在しない（has_archive => false）。/node-library/ で一覧できるようにする。

# 確定仕様（変更禁止。ここに書かれていない拡張は行わない）
1. plugins-embedded/node-library/node-library.php: has_archive => true に変更（アーカイブURLは /node-library/）。
2. リライトルール: flush_rewrite_rules() を init 等で毎回実行するのは禁止。調査で見つけた既存のバージョン管理/activationフックの仕組みに合わせて「一度だけ」flushされるようにする。既存機構がなければ、プラグインバージョン定数とDBオプション（例: node_library_flushed_version）を比較して一度だけflushする最小実装を追加する。
3. アーカイブテンプレート: card-library.php のカードとそのグリッドスタイルを流用して一覧表示する。供給方式（テーマ側 archive-node_library.php か、プラグイン側 template_include か）は、調査で判明した既存パターンと同じ方式を選ぶこと。独自の新方式を発明しない。
4. 一覧仕様: 公開（publish）のみ。表示順は既存のライブラリ一覧表示（ショートコード/ブロック等があればそれ）と同じ並び順に合わせる。ページネーションはテーマの既存アーカイブ（archive.php）と同じ見た目・仕組みを使う。
5. アーカイブヘッダー: inc/archive-helpers.php node_get_archive_context() の is_post_type_archive 分岐で node_library の場合のタイトル・サブタイトル・アイコン（'menu_book' 等ライブラリらしいMaterial Symbols名）が出るよう、必要最小限の分岐追加は可。
6. SEO: inc/seo.php のタイトル・パンくずがアーカイブページで破綻しないことを確認。変更が必要な場合のみ最小限の修正。

# 禁止事項（重要）
- 既存ファイルの全体リライト・整形・リネームは禁止。差分は今回の機能に必要な行だけに限定する。
- 既存のライブラリ単体ページ・カードショートコード・ブロックの挙動を変えない。
- 変更ファイルは node-library.php / アーカイブテンプレート1ファイル / inc/archive-helpers.php（必要時）/ inc/seo.php（必要時）の4ファイル以内。これを超える場合は作業を止めて報告する。
- npm / yarn / pnpm は使わない。ビルドが必要な場合は bun x vite build のみ（今回はCSS/JS非変更の想定なので原則ビルド不要。変更した場合のみ実行）。
- git commit / push はしない（コミットはユーザーと検証ゲートの判断）。

# 検証（実装後に必ず実行）
- 変更した全PHPファイルに php -l を実行し、結果を報告に含める。
- cybernode.local（/Users/saitoutatsuya/Local Sites/cybernode/app/public/）で確認する場合、テーマ/プラグイン同期後にパーマリンク再保存（またはflush発火）が必要。実施できない場合は「検証ゲートで必要な手順」として報告に明記する。

# 完了条件
- /node-library/ でライブラリ一覧がカードグリッドで表示され、ページネーションが動作する
- アーカイブヘッダー（タイトル・件数）が他のアーカイブと同じ見た目で表示される
- 既存のライブラリ単体ページ・カード表示に回帰がない
- 404にならない（リライトルールが正しくflushされる）

# 報告フォーマット
1. 調査結果: テンプレート供給方式（どのパターンに合わせたか）とflush発火点の判断根拠
2. 変更ファイル一覧と各ファイルの変更概要
3. php -l の結果
4. cybernode.localでの確認URL（http://cybernode.local/node-library/ と単体ページ1件）と、未実施の場合に検証ゲートで必要な手順
```

---

## ④ ライター別アーカイブ導線（ライターカード→著者アーカイブ） — 実装（Codex / GPT-5.5、制限中ならFable 5に引き継ぎ可）

```
Node テーマ（WordPress、/Users/saitoutatsuya/Documents/GitHub/Node）のv1.2向けに、記事下部のライター情報カードからライターごとのアーカイブ（著者アーカイブ）への導線を実装してください。小さいタスクなので一人で完走すること。

# 背景
記事下部のライター情報カード（template-parts/card-writer.php、#m3-writer-card、single/footer.php:27 から読み込み）には自己紹介とSNSリンクしかなく、そのライターの記事一覧へ飛ぶ手段がない。著者アーカイブの受け皿は既存（archive.php + inc/archive-helpers.php:55 の is_author 分岐、inc/seo.php:214 のタイトル対応、構造化データも seo.php:36 で get_author_posts_url 出力済み）なので、テンプレート変更は不要のはず。導線の追加が本タスクの中心。

# 確定仕様
1. template-parts/card-writer.php: ライターカード内（bio・SNSリンクの後）に著者アーカイブへのリンクボタンを追加する。
   - URL: get_author_posts_url($author_id)
   - ラベル: 「◯◯の記事一覧」（display_name使用）＋ 公開記事数（count_user_posts($author_id, 'post', true)）を「N件」として添える。
   - マークアップ: Material Symbols アイコン（article 等）＋テキストの、テーマ既存のtonalボタン調。既存クラスで流用できるボタンスタイルがあればそれを使い、なければ .m3-writer-card__archive-link を新設。
   - 記事数0件のライター（通常は起こらないが念のため）はボタンを出さない。
2. SNSリンクのピル化（2026-07-09追加・ユーザー確定）: 現在アイコンのみの丸ボタン（.m3-sns-icon）になっているSNSリンクを、**ピル型チップ（アイコン＋サービス名ラベル）**に変更する。
   - サポート対象サービス（card-writer.php の $sns_keys 参照: X(Twitter)・GitHub・YouTube・Facebook・Instagram）は**そのサービスのアイコンを表示**する。Material Symbolsにブランドアイコンは無いため、インラインSVG（simple-icons等のパスをテンプレート内に直接埋め込み。外部CDN読み込みは禁止）を使う。
   - カスタムリンク（custom_link_1〜5）はMaterial Symbolsの 'link' アイコン＋ラベルのピルとする。
   - ピルはM3のassist chip調（角丸・アウトラインor tonal、hover/フォーカス状態あり）。target="_blank" rel="noopener" は維持。
   - リンクが多い場合（最大10個）に折り返しても破綻しないこと。
3. CSS: src/styles/_writer-card.css に追加（ピル・アイコンサイズ・折り返し）。ライトモード/ダークモード両対応。既存のライターカードのアバター・bio構造は崩さない。
3. 著者アーカイブページの表示確認: /author/{nicename}/ が archive.php で正しく表示されること（アーカイブヘッダーのタイトル=ライター名、サブタイトル=著者、bio表示は archive-helpers.php:55-64 で実装済み）。必要なら is_author 分岐のアイコンを 'folder' からライターらしいもの（'person' 等）に変える最小修正のみ可。
4. ヒーロー（template-parts/single/hero.php）の著者チップ（.m3-hero-author、記事下カードへのページ内ジャンプ）は今回触らない。導線はライターカード側のみ。

# 制約（AGENTS.md準拠）
- 変更は card-writer.php / src/styles/_writer-card.css / inc/archive-helpers.php（アイコン変更時のみ）の3ファイル以内。
- 全体リライト禁止。SNSリンク・アバター・bio等の既存構造は変更しない。
- php -l で構文チェック。CSS変更後は bun x vite build を必ず実行。

# 完了条件
- 記事ページのライターカードに「◯◯の記事一覧（N件）」ボタンが表示され、クリックで /author/{nicename}/ に遷移する
- 著者アーカイブページがアーカイブヘッダー（ライター名・件数・bio）付きで正しく表示される
- SNSリンクがアイコン＋サービス名のピル型チップで表示され、サポート対象サービスはブランドアイコン、カスタムリンクはlinkアイコンになる
- リンク数が多くてもピルが自然に折り返す
- ライト/ダークモード両方でボタン・ピルの配色が破綻しない
- ライターカードの既存表示（アバター・bio）に回帰がない

# 報告
変更ファイル一覧、ビルド結果、cybernode.localでの確認URL（ライターカードのある記事1件と著者アーカイブページ）を報告すること。
```

---

## ⑤ ライブラリ新設計（ストアフロント風一覧＋ゲーム個別ページ） — 実装（Opus 4.8 / Fable 5、Claude Code）

```
Node テーマ（WordPress、/Users/saitoutatsuya/Documents/GitHub/Node）のv1.2向けに、ライブラリ（node_libraryカスタム投稿タイプ）の一覧と個別ページを完全新規のデザインで実装してください。このタスクは最初から最後まで一人で完走すること。ビジュアルの方向性以外のディテール（レイアウト・配色・モーション・レスポンシブ・ダークモード）はあなたの判断で品質を確保すること。

# 背景（③で基盤実装済み）
- has_archive => true / publicly_queryable => true / バージョン比較による一回だけのrewrite flush は実装・検証済み（node-library.php:745付近）。/node-library/（一覧）と /node-library/{slug}/（単体）はURLとして生きている。
- 現在の一覧（テーマの archive-node_library.php）はカテゴリアーカイブの部品（m3-archive-header＋card-library.php並べ）を流用した暫定実装。単体ページはテンプレートが無く、記事用single.phpで薄いページとして描画されてしまっている。
- ユーザー確定方針: **カテゴリアーカイブの流用をやめ、一覧・個別とも完全新規の設計にする。**

# 確定仕様
1. 一覧（/node-library/、archive-node_library.php を全面書き換え）: **ストアフロント風**のショーケース型デザイン。
   - ライブラリ専用のヘッダー（m3-archive-header は使わない。ストアの棚を思わせる専用の見せ方を新規に設計）。
   - タイプフィルター必須: すべて / ゲーム / アプリ（_node_library_type メタ: game/app）。実装方式（URLクエリでのサーバーサイド絞り込み or クライアントサイド切替）はページネーションとの整合を考えてあなたが判断する。
   - タイトル検索は任意（付ける場合はシンプルに）。
   - 各アイテムは個別ページ（/node-library/{slug}/）へリンクする。
2. 個別ページ（新規 single-node_library.php）: **ゲームごとの個別ページを新規設計**。
   - 内容: タイトル、タイプバッジ（ゲーム/アプリ）、紹介文（_node_library_summary）、ストアリンク（_node_library_links。platform/url/category/hardware を持つ。hardware別の整理やバッジ表示を活かすこと）、一覧へ戻る導線。
   - 記事内で使われている既存カード（plugins-embedded/node-library/templates/card-library.php）のトーン（Material 3・プラットフォームブランドカラー）と調和させつつ、個別ページとして情報量に耐える新レイアウトにする。
   - 可能なら「この作品に触れた記事」一覧を出す（ライブラリカードを埋め込んでいる記事の逆引きが既存メタ・ブロック属性から可能かを調査し、無理なく取れる場合のみ。取れなければ省略してよい）。
3. 記事内カードからの導線: card-library.php に個別ページへのリンク（「詳細を見る」等の控えめな導線）を追加する。カードの既存レイアウト・ストアボタンの挙動は壊さない。
4. パンくず・SEO: 個別ページのパンくずを「ホーム > Node Library > 作品名」にする（inc/seo.php node_the_breadcrumbs() の is_singular 分岐は現在カテゴリ前提なので、node_library の場合の最小分岐を追加）。タイトルタグ・OGPが破綻しないことを確認。
5. アーカイブヘッダー流用の撤去: ③で追加した inc/archive-helpers.php の node_library 分岐（menu_book等）は、新デザインで不要になるなら削除してよい（他のアーカイブに影響を出さないこと）。

# 制約（AGENTS.md準拠）
- 変更は archive-node_library.php / single-node_library.php（新規）/ 専用CSS（新規 _library-archive.css 等＋style.cssへの@import）/ card-library.php / inc/seo.php / inc/archive-helpers.php / JS最小1ファイル（フィルターに必要な場合のみ）まで。
- 記事内ライブラリカードのストアリンク・タブ・バッジ機能に回帰を出さない。
- ビルドは bun x vite build のみ。php -l / node --check で構文チェック。ライト/ダークモード両対応。

# 完了条件
- /node-library/ がストアフロント風の専用デザインで表示され、タイプフィルターが機能し、ページネーションが破綻しない
- 各アイテムから個別ページに遷移でき、個別ページがゲーム情報（紹介文・ストアリンク・ハード別整理）を新デザインで表示する
- 記事内のライブラリカードから該当の個別ページへ遷移できる
- パンくずが「ホーム > Node Library > 作品名」になる
- カテゴリ等の既存アーカイブ・記事内カードに回帰がない
- ライト/ダークモードとも破綻しない

# 報告
変更ファイル一覧、ビルド結果、cybernode.localでの確認URL（一覧・フィルター適用・個別ページ2件（game/app各1）・カード埋め込み記事）を報告すること。
```

---

## ⑥ 日付アーカイブ新設計 — 実装（Opus 4.8 / Fable 5、Claude Code）

```
Node テーマ（WordPress、/Users/saitoutatsuya/Documents/GitHub/Node）のv1.2向けに、日付アーカイブ（年/月/日、/2026/07/ 等）を完全新規のデザインで実装してください。このタスクは最初から最後まで一人で完走すること。

# 背景・確定方針
- 現在、日付アーカイブは汎用 archive.php（カテゴリアーカイブと同じ m3-archive-header＋カード列）で描画される。受け皿のコンテキスト（inc/archive-helpers.php:65-79 の年/月/日分岐）は存在する。
- ユーザー確定方針: **カテゴリアーカイブの流用をやめ、日付アーカイブ専用の新規デザインにする。デザインの方向性（タイムライン型・カレンダー型等）はあなたに一任する。**「時間を遡る」体験として自然で、Nodeの Material 3 Expressive のトーンに合うものを設計すること。

# 確定仕様
1. 新規テンプレート date.php を作成し、年・月・日アーカイブを専用デザインで描画する（archive.php はカテゴリ等の既存アーカイブ専用のまま触らない。テンプレート階層で date.php が優先されることを利用する）。
2. 年↔月の相互ナビゲーション（前後の月・年への移動、その年の月一覧など、粒度間の行き来）を設計に含める。記事が存在しない期間はリンクを出さない。
3. ページネーションは既存アーカイブと同等の使い勝手を保つ（見た目は新デザインに合わせてよい）。
4. 導線: 日付アーカイブへの入口を最低1箇所実装する（候補: 記事ヒーローの日付をリンク化 / フッターのアーカイブリスト。どちらにするか・見せ方はあなたが判断。ヒーロー日付リンク化を選ぶ場合は②の実装と衝突しないよう、②完了後のhero.phpに対して最小差分で行うこと）。
5. SEO: 年/月/日のタイトルタグ・パンくず（inc/seo.php は is_year/is_month/is_day 対応済み）が新テンプレートでも正しく出ることを確認。

# 制約（AGENTS.md準拠）
- 変更は date.php（新規）/ 専用CSS（新規＋style.cssへの@import）/ 導線1箇所のテンプレート / inc/archive-helpers.php（必要時）/ JS最小（必要時のみ）まで。
- カテゴリ・タグ・著者アーカイブ（archive.php系）に一切影響を出さない。
- ビルドは bun x vite build のみ。php -l で構文チェック。ライト/ダークモード両対応。

# 完了条件
- /2026/ /2026/07/ 等が専用の新デザインで表示され、年↔月のナビゲーションが機能する
- 記事のない期間のリンクが出ない・404にならない
- 導線（最低1箇所）から日付アーカイブに到達できる
- カテゴリ等の既存アーカイブの表示が一切変わっていない
- ライト/ダークモードとも破綻しない

# 報告
採用したデザインの方向性と理由、変更ファイル一覧、ビルド結果、cybernode.localでの確認URL（年・月アーカイブ各1、導線の設置箇所）を報告すること。
```

---

## 検証・リリースゲート（Claude Code、全タスク完了後にまとめて）

```
Node テーマ（/Users/saitoutatsuya/Documents/GitHub/Node）のv1.2 Round 3（①ボトムバー排除＋トップFABのみモバイル表示 ②ヒーロー縦分割＋目次統合＋サムネ見切れ解消 ③ライブラリアーカイブ基盤 ④ライター別アーカイブ導線＋SNSピル ⑤ライブラリ新設計（ストアフロント一覧＋ゲーム個別ページ） ⑥日付アーカイブ新設計、実装プロンプトは 1.2round3-prompts.md）を検証し、リリース可否を判断してください。①と②はセットでないとモバイルの目次導線が欠けるため、片方だけのリリース合格は出さないこと。

# 確認すること
1. git diff / git log で3タスクの変更内容を確認し、1.2round3-prompts.md の各完了条件からの逸脱がないかレビューする。特に「新規データ導入なし（訴求ヘッダー・関連情報リスト未実装が正）」を確認。
2. bun x vite build → cybernode.local（/Users/saitoutatsuya/Local Sites/cybernode/app/public/wp-content/themes/node/）へrsync同期、同期後 plugins-embedded/ を削除（二重読み込み防止）。③はプラグイン側変更のためプラグイン同期とパーマリンク再保存も行う。
3. 実環境確認（報告には必ず記事URL・編集URLを貼ること）:
   - ①: モバイル幅でボトムバー不在・トップFABのみ表示・動作・下部余白。PC不変（目次FAB・追従パネル含む）。
   - ②: PC 2カラムヒーロー、横長/縦長/正方形サムネの全体表示、サムネ無し記事、ダークモード、モバイル1カラム。読了バッジ展開とコメントアンカーの動作。モバイルのヒーロー内プルダウンからのジャンプ、追従バーの出現・現在地同期・タップ操作、マルチページ記事でのページ単位グループ化とページ遷移、見出し無し記事での目次UI非表示。
   - ③+⑤: /node-library/ のストアフロント一覧・タイプフィルター・ページネーション、個別ページ（game/app各1）の表示・パンくず、記事内カードからの遷移、既存カード機能（ストアリンク・タブ・バッジ）の回帰なし。リライトルールflushの自動発火（③で検証済みだが⑤のテンプレート差し替え後も404が出ないこと）。
   - ④: ライターカードの記事一覧ボタン表示・遷移、SNSピル（ブランドアイコン/linkアイコン・折り返し）、/author/{nicename}/ のアーカイブヘッダー（ライター名・件数・bio）、ライト/ダーク両対応、既存カード表示の回帰なし。
   - ⑥: 年・月アーカイブの新デザイン表示、年↔月ナビゲーション、記事のない期間のリンク非表示、導線からの到達、カテゴリ等既存アーカイブの不変。
4. PHPUnit全件パス、php -l、既存機能（シリーズ・ブログカード・node/notice・追記日・プライマリカテゴリ）への回帰がないこと。
5. AGENTS.md の動作確認チェックリストに準拠。

# 判断
すべて問題なければ 1.2featurelist.md にRound 3の完了節を追記し、CHANGELOG.md追記・リリース手順への合流可否をユーザーに報告する。問題があれば担当エージェント（①=Claude Code（Codexから引き継ぎ済み）、②⑤⑥=Opus/Fable 5、③=Gemini 3.1 Pro（AntiGravity）、④=Codex（制限中ならFable 5））への差し戻し指示を作成する。コミット・push・ZIP生成は必ずユーザー確認を取ってから行うこと。
```
