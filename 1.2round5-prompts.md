# Node v1.2 Round 5 — 未消化項目の消化（テスト+残実装）計画

## Context
7/12の監査で確定した未消化項目を消化する。対象: ①クラシックエディタでのシリーズ検証（Round 1持ち越し）②Node Library回帰スイート実装（NODE_LIBRARY_REGRESSION_PLAN.md未実装）③構造診断の凍結テスト（T-1/T-8/T-11）④F-4 node-flow RESTホワイトリスト化 ⑤F-9 シリーズ目次の順序メタ無し記事包含。
計画者=Fable 5。実行は各タスクを**別セッション・1タスク1目的**で Opus 4.6 / Opus 4.8 / GPT-5.5 に配布する。プロンプトはモデル非依存で自己完結（セッション文脈を前提にしない）。


## 実行順と担当

| # | タスク | 担当（推奨） | 種別 |
|---|---|---|---|
| ⑤ | F-9 順序メタ無し記事の目次包含 | Opus 4.6 | 実装+テスト |
| ④ | F-4 node-flow RESTホワイトリスト化 | Opus 4.8 | 実装+テスト（判断あり） |
| ③ | 凍結テスト T-1/T-8 + route-check.mjs | GPT-5.5 | テストのみ |
| ② | Node Library回帰スイート | Opus 4.6 | テスト基盤実装 |
| ① | クラシックエディタでのシリーズ検証 | GPT-5.5 | 検証のみ（コード変更なし） |
| ゲート | 全件の検証・リリースゲート | Claude Code（Fable 5） | 検証 |

順序根拠: ⑤→③の順にすると T-5追補（⑤に含む）と T-1/T-8 が独立に書ける。④は他と衝突しない。②は長め、①は環境準備（Classic Editorプラグイン導入）だけ先行可。

## 全プロンプト共通の前提（各プロンプト冒頭に埋め込み済み）
- リポジトリ: /Users/saitoutatsuya/Documents/GitHub/Node（AGENTS.md必読）
- ビルド: `bun x vite build` のみ。php -l / node --check 必須。**git commit/push禁止**
- LocalWP: サイト http://cybernode.local 、テーマ同期は rsync＋`plugins-embedded/`削除＋Super Cacheクリア。プラグインは `wp-content/plugins/node-*` → `plugins-embedded/*` の**symlink**（プラグイン編集は即反映）
- PHPUnit: `NODE_TEST_DB_SOCKET="/Users/saitoutatsuya/Library/Application Support/Local/run/Q39UjXsTt/mysql/mysqld.sock" composer test`（現行52件全green維持が回帰条件）
- WP CLI相当: LocalWPのphpバイナリ `/Users/saitoutatsuya/Library/Application Support/Local/lightning-services/php-8.2.30+1/bin/darwin/bin/php -d mysqli.default_socket=<socket>` で wp-load.php をrequire

---

## プロンプト⑤ — F-9: シリーズ目次の順序メタ無し記事包含（Opus 4.6）

```
Node テーマ（WordPress、/Users/saitoutatsuya/Documents/GitHub/Node）のv1.2向けに、シリーズ（node_series）目次クエリの「順序メタ無し記事の静かな脱落」を修正してください。一人で完走すること。着手前に AGENTS.md と STRUCTURAL-REVIEW-1.2.md の F-9 節を読むこと。

# 背景
plugins-embedded/node-series/node-series.php の node_series_get_posts() は meta_key（_node_series_order）指定のINNER JOINで記事を取得しているため、クイック編集や標準タクソノミーUI経由でシリーズに割り当てられた「表示順メタを持たない記事」が目次・話数計算から静かに脱落し、「自分が載っていない目次」が表示される。

# 確定仕様
1. node_series_get_posts() のクエリを meta_query の relation=OR（EXISTS / NOT EXISTS）に変更し、並び順は「メタ値昇順 → 日付昇順」を維持。メタ無し記事は末尾に置く。
2. 既存の並び（全記事メタあり）の挙動は完全に不変であること（既存PHPUnitが回帰網）。
3. tests/node-series-test.php に追補（STRUCTURAL-REVIEW-1.2.md T-5の該当ケース）:
   - wp_set_object_terms 直（クイック編集相当）で追加したメタ無し記事が目次データに末尾で含まれ、その記事から見て is_current が立つ
   - 下書き混在時のposition/total整合、記事削除時のtotal追随（既存テストがあれば流用）
   - node_series_get_adjacent/position/toc_data/color の戻り値スキーマ（キー名・型）のアサート（API凍結。v1.3ボトムナビが消費する契約）

# 制約
- 変更は node-series.php / tests/node-series-test.php の2ファイル以内。全体リライト禁止。
- php -l 必須。PHPUnitは NODE_TEST_DB_SOCKET="/Users/saitoutatsuya/Library/Application Support/Local/run/Q39UjXsTt/mysql/mysqld.sock" composer test で全件（52件+追補）green。
- CSS/JS非変更なので bun x vite build 不要。git commit / push はしない。

# 完了条件
- メタ無し記事が目次・前後ナビ・話数計算に末尾で含まれる
- 全記事メタありのシリーズの並び・表示が完全に不変
- composer test 全green（既存52件+追補）

# 報告
変更差分の要約、追加テストのケース一覧、composer test の結果を報告。cybernode.local の確認URL（シリーズ記事1件: http://cybernode.local/yugioh-series-3/ 等）を含めること。
```

---

## プロンプト④ — F-4: node-flow RESTクエリのホワイトリスト化（Opus 4.8）

```
Node テーマ（WordPress、/Users/saitoutatsuya/Documents/GitHub/Node）のv1.2向けに、node-flowプラグインのREST入力面を閉じる修正を実装してください。一人で完走すること。セキュリティと後方互換の判断を要するためOpus担当。着手前に AGENTS.md と STRUCTURAL-REVIEW-1.2.md の F-4 / T-9 節を読むこと。

# 背景
plugins-embedded/node-flow/includes/Frontend/Scroller.php の REST エンドポイント node-flow/v1/posts が、クライアント任意の query 配列を WP_Query に直マージしている（meta_query / post_type / post_status 等を外部から注入可能）。本番稼働中のためv1.2ブロッカー扱い。

# 確定仕様
1. get_posts_html() で許可キーのみ抽出するホワイトリスト方式に変更。許可キーは get_current_query_vars()（フロント側JSが実際に送るもの）を調査して確定すること（想定: cat, tag, s, author, year, monthnum, day, node_series, paged 等）。
2. post_status=publish の強制は維持。post_type は post 固定（外部指定を無視）。
3. tests/ に T-9 相当のPHPUnitを新規追加（tests/node-flow-rest-test.php 想定）:
   - query[meta_key] / query[post_type]=page / query[post_status]=draft が無視される
   - query[cat] / query[s] / query[tag] は機能する
   - 下書きタイトルがレスポンスHTMLに漏れない
   - page=2 の hasMore が総数と整合
4. 既存アーカイブ（カテゴリ・タグ・検索・日付・シリーズ）での無限スクロールが従来どおり動くこと（許可キー漏れによる絞り込み欠落が最大リスク。調査を先に行い、報告に許可キー一覧と根拠を書くこと）。

# 制約
- 変更は Scroller.php / tests/ の2ファイル以内。全体リライト禁止。php -l 必須。
- PHPUnit: NODE_TEST_DB_SOCKET="/Users/saitoutatsuya/Library/Application Support/Local/run/Q39UjXsTt/mysql/mysqld.sock" composer test 全green。
- git commit / push はしない。プラグインはLocalWPへsymlink済みのため、cybernode.localでの動作確認は同期不要で可能（テーマ側は変更しない）。

# 完了条件
- 非許可キーの注入が無効化され、既存の無限スクロール（カテゴリ/タグ/検索/日付アーカイブ）に回帰がない
- composer test 全green

# 報告
許可キー一覧と決定根拠、テスト結果、cybernode.localで無限スクロールを確認したアーカイブURL（カテゴリ・検索の最低2種）を報告。
```

---

## プロンプト③ — 凍結テスト T-1 / T-8 / route-check.mjs（GPT-5.5）

```
Node テーマ（WordPress、/Users/saitoutatsuya/Documents/GitHub/Node）のv1.2向けに、実施済み修正の「後退防止テスト」を追加してください。一人で完走すること。実装仕様は STRUCTURAL-REVIEW-1.2.md の T-1 / T-8 / T-11 節に確定済み。AGENTS.md も必読。**プロダクションコードは一切変更しないこと（テスト・スクリプト追加のみ）。**

# 背景（対象の修正は実施済み）
- F-1: plugins-embedded/node-series/node-series.php にバージョン比較の一回だけ rewrite flush が実装済み（option: node_series_flushed_version）
- F-2: inc/blogcard.php node_resolve_redirect() の sslverify=false は撤回済み（既定true）

# 確定仕様
1. tests/node-series-test.php に T-1 を追補:
   - go_to('/series/{slug}/') で is_tax('node_series') が立ち is_404 でない（$wp_rewrite->set_permalink_structure('/%postname%/') → タクソノミー登録 → flush の順で準備）
   - 未知スラッグ /series/no-such/ は is_404
   - flush option が現行値のとき flush_rewrite_rules が再実行されない（option値アサート）
2. tests/node-sslverify-guard-test.php を新規追加（T-8）:
   - リポジトリ内PHP（node_modules/vendor/scratch/.tmp/tests除外）を走査し、'sslverify' => false の出現が既存許容3件（inc/blogcard.php のOGP取得 / plugins-embedded/luminous-nexus/includes/shortcode-blogcard.php / plugins-embedded/node-library/node-library.php）と完全一致することをアサート
   - node_resolve_redirect() を pre_http_request フィルタで捕捉し、sslverify が true（既定）で呼ばれることをアサート
3. scripts/route-check.mjs を新規追加（T-11、bunで実行: bun scripts/route-check.mjs）:
   - cybernode.local に対し次のURL表を fetch でステータス検査: /=200, /all-articles/=200, /headlines/=200, /spotlight/=200, /category/spotlight/=301→/spotlight/, /series/{存在するslug}/=200, /?s=node=200, /feed/=200, /no-such-xyz/=404, /wp-sitemap.xml=200
   - 301は1ホップで200到達・5xxゼロを合格条件に、結果を表形式で出力し、不一致があれば exit 1
   - シリーズslugは実行時に /wp-json/wp/v2/ 等から動的取得するか、引数で渡せるようにする（ハードコード禁止）

# 制約
- 追加は tests/ 2ファイル＋scripts/route-check.mjs の3ファイル以内。既存テスト・既存スクリプトの変更禁止。
- PHPUnit: NODE_TEST_DB_SOCKET="/Users/saitoutatsuya/Library/Application Support/Local/run/Q39UjXsTt/mysql/mysqld.sock" composer test で全件green。
- route-check.mjs は LocalWP（cybernode.local）起動中に実行して全行一致を確認。node --check で構文チェック。
- git commit / push はしない。bun x vite build 不要（src非変更）。

# 完了条件
- composer test 全green（既存52件+追補）
- bun scripts/route-check.mjs が全行一致で exit 0

# 報告
追加テストのケース一覧、composer test / route-check の実行結果（全行の実測ステータス表）を報告。
```

---

## プロンプト② — Node Library回帰スイート（Opus 4.6）

```
Node テーマ（WordPress、/Users/saitoutatsuya/Documents/GitHub/Node）向けに、NODE_LIBRARY_REGRESSION_PLAN.md（リポジトリ直下、正本）に基づく Node Library カードの回帰テストスイートを実装してください。一人で完走すること。AGENTS.md 必読。**Node Library本体のコードは変更しない（テスト基盤の追加のみ）。**

# 実装物（正本の Implementation Notes どおり最小構成）
1. フィクスチャ生成スクリプト scripts/library-fixtures.php:
   - LocalWPのphpバイナリ（/Users/saitoutatsuya/Library/Application Support/Local/lightning-services/php-8.2.30+1/bin/darwin/bin/php -d mysqli.default_socket="/Users/saitoutatsuya/Library/Application Support/Local/run/Q39UjXsTt/mysql/mysqld.sock"）で wp-load.php をrequireして実行する開発用スクリプト
   - 正本の Test Matrix に対応する固定スラッグの記事（node-library-regression-steam-only / steam-mixed / console-mixed / mobile-apps / invalid-links）を決定的に再作成（存在すれば削除→再作成）。ライブラリカードは既存のブロック/ショートコード形式（plugins-embedded/node-library/templates/card-library.php が描画するもの）を調査して正しいマークアップ/メタで投入すること
2. Playwright回帰スクリプト scripts/library-regression.mjs（bun + リポジトリ既存の playwright を import）:
   - 各フィクスチャページで正本の Playwright Checks を実施: タブ切替・可視プラットフォームボタンのラベル/href・ハード別注記（(PS4)等）・Steam埋め込みトグルon/offのピル表示・任天堂/PS/Xboxの警告表示とタイムアウト・空ピルなし・モバイル幅(390px)で横はみ出しなし・console errorゼロ
   - 代表ケースのスクリーンショットを scratch/library-regression/ に保存（リリース証跡用）
   - 不一致は exit 1 で失敗させ、失敗項目を一覧出力
3. HOW_TO_RELEASE.md に「Node Libraryコード変更時は本スイートをZIP生成前に必須実行」の1節（実行コマンド付き）を追記

# 制約
- 追加は scripts/ 2ファイル＋HOW_TO_RELEASE.md の3ファイル以内。プラグイン本体・テーマ・既存テストの変更禁止。
- 実行前提: bun x vite build → cybernode.localへrsync同期 → 同期先の plugins-embedded/ 削除 → Super Cacheクリア（rm -rf "/Users/saitoutatsuya/Local Sites/cybernode/app/public/wp-content/cache/supercache/"*）。この手順もスクリプトのヘッダコメントに明記。
- node --check / php -l 必須。git commit / push はしない。

# 完了条件
- フィクスチャ再作成→回帰スクリプト実行が全項目passし、代表スクリーンショットが保存される
- 既存のライブラリ記事・実運用データに影響を与えない（フィクスチャは専用スラッグのみ操作）

# 報告
Test Matrixの網羅状況（実装したチェック項目の一覧と、初版で意図的に省いた項目があればその理由）、実行結果、スクリーンショットパス、cybernode.localのフィクスチャURL一覧を報告。
```

---

## プロンプト① — クラシックエディタでのシリーズ検証（GPT-5.5、検証のみ）

```
Node テーマ（WordPress、/Users/saitoutatsuya/Documents/GitHub/Node）のシリーズ機能を、クラシックエディタ環境で検証してください。**検証タスクであり、コード変更は一切禁止**（不具合を発見したら修正せず報告に留める）。AGENTS.md 必読。

# 背景
シリーズの「シリーズ内の表示順序」メタボックス（plugins-embedded/node-series/node-series.php）はGutenberg実保存とAjax連動まで検証済みだが、クラシックエディタ（ブロックエディタ未使用）での表示・保存が未確認のままv1.2の残課題になっている（1.2featurelist.md ①節）。

# 手順
1. cybernode.local（LocalWP。停止していたらユーザーに起動を依頼）の wp-admin に Classic Editor プラグインを導入・有効化する（wp-content/plugins/ へのWP標準インストール。既に入っていれば有効化のみ）。
2. CDP（headless Chrome）等で wp-admin にログインし、クラシックエディタで次を確認:
   a. 投稿編集画面に「シリーズ内の表示順序」メタボックスが表示される（登録先シリーズ・表示順の両プルダウン）
   b. シリーズ選択時のAjax再計算（表示順候補・件数）が動く
   c. 上限到達済みシリーズ・使用済み表示順が選択不可になっている
   d. 保存でメタ（_node_series_order）とterm割当が正しく反映される（保存後に再度開いて値が保持されていること、DB上のメタ値も確認）
   e. リビジョン/自動保存で値が壊れない（下書き保存→本保存の往復）
   f. 保存した記事のフロント表示（シリーズ目次・前後ナビ・カードのN/Nバナー）が正しい
3. 検証後、Classic Editor プラグインを**無効化**して環境を元に戻す（アンインストールまでは不要）。
4. 使用した検証記事はタイトルに【検証】を付け、既存記事を書き換えないこと。

# 報告
a〜fの各結果（スクリーンショット付き）、確認した記事URL・編集URL、発見した不具合（あれば再現手順つき）。問題ゼロなら 1.2featurelist.md ①節の残課題行の消し込みを提案として報告に含める（編集はしない）。
```

---

## 最終ゲート（Claude Code / Fable 5、全タスク完了後）

1. composer test 全green（52件+⑤④③の追補）・bun scripts/route-check.mjs・Node Library回帰スイートの3点を通す
2. ①の報告を受けて 1.2featurelist.md の残課題行を消し込み、Round 5節に結果を記録
3. F-4/F-9はSTRUCTURAL-REVIEW-1.2.mdの該当行に「実施済み」を追記
4. コミット・push・CHANGELOG・ZIPは従来どおりユーザー確認後

